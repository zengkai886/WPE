# 数据接线增量审查

基线：`4f1e880c678c56e1faba9039e9bb258805e2f0be`。审查对象为其后的工作区增量及新增文件，不是整套程序完成认证。需求来源为用户更新版工程文档与原始源码。

## Standards

两项 P2 问题，均已修复并由审查代理复核：

1. **退出逐个执行积压数据库任务**：UI 线程析构 join 会被每个任务的锁等待叠加，三个请求复现约 3037ms。现在取消尚未开始的任务，仅等待正在进行的事务结束；锁库回归两条取消，最新 x64/Win32 分别 979/981ms。
2. **单次通知/回调异常丢失同批其他完成结果**：原 Drain 出错后可使两个请求都无法完成。现在逐结果、逐通知及逐完成回调隔离，已提交但通知失败明确报错；测试确认两个完成结果均收到，抛异常回调也不阻断后续结果。

限定范围复核未发现新的生命周期问题。维护性与 `/W4 /WX` 构建通过不能代替全部业务验证。

## Spec

三项 P2 行为不一致，均已修复并由审查代理独立复核：

1. **仓库预览 DTO 不符**：原 `getStorePreviews` 返回 `items: string[]`，初版错误返回行对象。已还原字符串数组，并使用非空 BLOB 测试。
2. **名称空白处理不符**：ASCII trim 未覆盖原 .NET 的全角空格、NBSP 等。已按 Framework 4.8 空白集合处理 UTF-16 边界，原程序集生成的 120 条参照扩充了此类输入。
3. **十六进制文本不符**：原版长预览追加 ` ...`；复制每一条都追加 CRLF，包含空 BLOB。已逐字节对齐并补边界回归。

原始总需求仍为**部分完成**：实际抓包/代理/执行器、剩余 160 个 RPC、其余推送源、完整导入导出等尚未实现。部分已注册方法也只有设置/列表或离线分支。详细边界见 `docs/数据接线进度.md`，不将该范围缺口伪装为审查通过后自动消失。

## 验证证据

- 最新全新目录测试：`evidence/data-run-manifest.json`、`data-full.log`。
- 每架构 6/6 CTest、440 条数据检查，保留协议/队列全部对拍。
- 真正原 Vue 新增/编辑/启用/取消删除与重启恢复：`data-host-self-test.json`、`data-host-restart.json`。
- 原程序测试加载器读取 C++ 数据库：`data-original-read-native-db.json`、同名 `.log`。
- 修复前失败证据：`data-preview-red.log`、`data-worker-red.log`、`data-unicode-red.log`。
- 早期隐藏窗口置顶偶发失败：`data-earlier-topmost-failure.json`。后续完整测试通过但根因未定位，窗口稳定性未全量验收。

**汇总：Standards 2 项 P2 已修复；Spec 3 项 P2 已修复。两轴分别复核，不代表全部工程需求已经达成。**

## 发布复验补充

本地发布检查发现源码 ZIP 无 `.git` 时原构建脚本会中止。已将 Git 改为可选来源记录，并对固定哈希的契约/黄金数据/生成文件禁用自动换行转换。此后从全新解压目录运行完整双架构测试与原 Vue 首启/重启，全部通过（`data-archive-run-manifest.json`）。这项脚本/归档修正由主代理实测，未声称经过前述两位审查代理的再次审查。

运行包使用该构建的实际 exe，包内 215 个原静态文件逐个哈希一致；不指定 `--assets` 的独立包自测通过；本机依赖检查仅列出系统 DLL，不依赖原 C# 业务程序集。WebView2 Runtime 仍为外部依赖。测试退出检查无本轮遗留宿主或匹配 profile 的 WebView2 进程，见 `data-cleanup.json`。
