# 首批 C++ 协议库双轴审查

审查基点：`63701d5`；被审查实现：`79d342e8f70a4a4de66c0b9663de92c098b4523b`。源码以更新版用户文档为准。两个审查代理分别审查 Standards 与 Spec，未修改交付文件。

## Standards

发现 2 项，均由主代理修复并重新测试：

1. **P2：空架构列表导致假通过。** `-Architecture @()` 会绕过所有 C++ 编译和对拍，却把运行标为通过。现增加非空/数量验证、重复架构检查，并在结束前核对成功架构数，空矩阵不得通过。
2. **P3：默认构建目录违反源码树外构建约定。** 原默认 `.build` 位于源码树内。现要求显式传入外部 `-BuildRoot`，拒绝源码根目录或子目录。

回归：`tests/script_validation.ps1` 的 4 项错误参数测试通过，在创建输出前拒绝空矩阵、重复架构以及两种源码树内输出路径；随后正常双架构构建与全部协议测试再次通过。审查代理独立复跑的是修复前的正常路径，修复后的参数及正常路径由主代理复测，不混淆这两项证据。

## Spec

**已声明的协议库增量内没有发现可操作的规格符合性问题。**

- LE、长度前缀、null/空、GUID 混合字节序及 Framework UTF-8 替代语义与原版一致。
- 分帧将头和内容一次交给写操作；正确区分正常 EOF 与截断，分配载荷前检查长度。
- 封包字段、64 位时间/socket、同内容省略位及消息编号与原版一致。
- 没有另写 Qt 界面，也没有加入 Java 或生产用 C# 后端。未实现的宿主、注入、队列、真实管道和业务处理没有被称为已完成。

独立验证：x64 / Win32 各通过 74,684 条原版数据、76,618 条断言、600 个原 C# 反向解码封包及 2 项 CTest。直接与用户 ZIP 比较 555 个前端/静态文件和两份 C# 参考文件，差异为 0。

**汇总：Standards 发现 2 项（最高 P2），已修复并由主代理复测；Spec 发现 0 项。审查不代表完整产品或 M0～M8 验收完成。**
