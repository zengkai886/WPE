# 配置加密接线 · 双轴审查

比较基线：`61022ed32850ddd14d01eaeb377e77bf36a0f3be`。范围为本切片新增/修改源码、原程序测试 oracle、配置 fixtures 和宿主接线，不是完整软件验收。

## Standards

独立审查报告：当前未发现剩余可行动问题，无另报的可选 smell。

- 原明文 prepare 完成后未登记 token，排队 apply 在导航取消后仍能改库。现在所有成功 prepare 均登记 token，清理优先入队；持有 SQLite 写锁的独立复现从仍导入三行变为不执行、零行。
- 父列表不再经 XML 重序列化中转封包，混合字段 `<Buffer>AA<X/>BB</Buffer>` 保留为 AA BB。
- 独立 SQLite BEFORE INSERT 故障注入确认五组备份的数据库事务整体回滚，内存语言也保持不变。
- 密码接口仅使用当前原生路径/内容 grant；桥失败不降级明文；加密前不写明文文件。

该轴独立构建阶段检查 172 项通过，后续主测试增加更多回归。原审查不宣称完整 GUI/双架构完成，后者由最终构建记录单独证明。

## Spec

独立审查发现并修复四项行为偏差：

1. 旧发送/机器人编辑会话在备份替换后保存，不应让旧导出计划重新绑定相同 GUID 的新父对象。保存改从 live 对象保留 `_objectId`，两种编辑器均有回归。
2. 子树重写丢失祖先 namespace；改为直接读取 XmlNode 的 namespace 与聚合文本，原版前缀容器样本逐字节通过。
3. 尾分隔符空字段不能丢弃。`Intercept,` 和九段末尾空 Function 按原 Enum.Parse/String.Split 失败回退，而非错误当成有效枚举/八段旧格式。
4. 父列表加载器不能额外限制根名称；原加载器直接遍历 Root.Elements。备份仍按原 Root.Name.LocalName 判断，分节仍按无 namespace 名称选择。

最终有界复核报告：根名修复通过，无新增发现；原程序集根名黄金 SHA256 一致；当前 x64 专项 **198 checks 全通过**。对应原始审查记录在工作区 `work/config-spec`、`work/config-standards`，关键结果随构建证据归档。

## 结论与未验收范围

Standards 两项发现已修复、当前零项；Spec 四项发现已修复、当前零项。两轴结论保持独立，不替代功能范围核对。

当前只支持十四种备份分组中的五种；其余配置/表、抓包/注入/Hook、代理、滤镜/发送/机器人真实执行等仍有大量开发工作。四父列表/备份的所有可见按钮组合和 Windows 选择框交互尚未做完整 UI 验收。TopMost 隐藏后台请求仍有失败，未修复根因。这些都不得借本次局部审查称为全体验一致。
