# 宿主与队列增量双轴审查

基点 `a9dc07e`，首次实现 `c640546`。两个审查代理分别审查 Standards 和 Spec；修复后执行新的双架构全量测试。

## Standards

发现 3 项 P2，均已修复；审查代理复核后无未解决项：

1. **取消时的重入 Ask 没被结束。** 完成回调又发起 Ask，会把新请求留在即将离开的页面。增加取消状态，取消期间的新请求不发送并返回安全默认值。回归先失败后通过；独立复测结果为 `pending=0, first completed=1, second completed=1, asks posted=1`。
2. **WebView2 回调内直接开模态错误框。** 可能进入不支持的嵌套消息循环。改为锁定首次错误并投递原生窗口消息，回调返回后再显示错误与退出，避免重复弹框。
3. **普通启动失败却返回 0。** 原 `WM_DESTROY` 忽略非自测模式的错误码。改为普通启动初值 0、自测初值 1；故障置 1，退出始终传回记录值。

另补充正常启动 4 秒显示兜底/30 秒超时，以及销毁前完成桥回调、异步完成处理器的生命周期检查。

普通模式人工关闭错误弹窗后的退出路径经过代码审查，**未声称人工界面测试通过**。额外真实导航故障注入的工具命令被拦截，未执行，不纳入通过证据。

## Spec

发现 1 项 P2，已修复；独立复核无未解决项：

- **桥接消息错误套用了 1 MiB 上限。** 原 C# WebBridge 没有该限制；它会静默丢失合法的大型 call/answer。已移除这条额外限制，并加入大于 1 MiB 的请求与回答回归。独立复测从 `answers=0 pending=1` 变为 `answers=1 pending=0`。也移除了未经原版合同约定的等待请求数量上限。

队列的逻辑大小、引用身份、丢旧保新、单条超限、批次预算和累计丢弃与原版对照一致。前端文件保持原样；明确未实现的后续业务不作为“已完成”呈现。

## 最终验证

- x64 / Win32 各通过 74,684 条原协议数据、76,618 条断言、600 个原版反向验证封包。
- 各通过 6,416 条原队列状态数据、并发守恒与退出唤醒测试。
- 各通过 4/4 CTest，包含大消息和取消重入回归。
- x64 宿主真实加载原 Vue，完成窗口置顶往返、未知方法报错、原 Vue 确认框回答及通知事件；自动退出码 0。
- 555 个原前端/静态文件哈希仍全部一致。
- 最终报告：`evidence/host-ring-run-manifest.json`，截图：`evidence/original-vue.png`。

**汇总：Standards 3 项 P2、Spec 1 项 P2，均修复。完整抓包/代理/数据库/全部 GUI 业务不在本次通过范围内。**
