# WPE C++ 无人值守接续

更新：2026-09-18。本文件是开发状态记录，不是已完成交付声明。

## 用户要求与运行方式

- 用户要求完整实现原版功能与体验，保留全部原 Vue，完成检查、修复、优化后再交付；不再等待用户回复“继续”。
- 当前任务已通过应用工具创建 heartbeat 自动接续 `wpe`，名称“WPE 原版功能无人值守移植”，ACTIVE，每 10 分钟接续检查。不是另外的新任务。常规推进保持安静，只在全部验收完成或必须用户介入时通知。
- 每轮实际推进代码与测试，记录下一步；不要反复仅审计、打包局部版本或者空报进度。不能承诺一夜完成，不能把接口注册当成功能完成。
- 电脑、Codex、网络和账户额度是运行条件。自动任务存在不等于可以在关机后执行。

## 路径与输入

- 项目：`E:\codex\2026-09-17\bi-a\outputs\WPE64-Cpp`。
- 中间文件与构建：`E:\codex\2026-09-17\bi-a\work`；交付只放 `outputs`。
- 原 ZIP：`C:\Users\Administrator\Downloads\WinsockPacketEditor-master.zip`，SHA256 `1F524B84621F064ACD91C3F9D764D1ADDF19AC42019E7FCEDE11BF55EA536113`。
- 更新文档：`docs/用户提供-重写工程文档-更新版.md`，以 `contracts/active-spec.json` 为准。
- 原源码：`E:\codex\2026-09-17\bi-a\work\source-audit\WinsockPacketEditor-master\WinsockPacketEditor`，重点 `Operate.cs`、`ShellForm.cs`。
- 原业务程序集：`E:\codex\2026-09-17\bi-a\work\baseline-build\WinsockPacketEditor-master\WinsockPacketEditor\bin\Release\WinsockPacketEditor.exe`，SHA256 `76C33B88188F7F921AE5B21117533FF1D3F1861A04A723B8087DE42B17E5FEED`。只用于测试，不是产品依赖。
- 历史已验证基线 commit `61022ed32850ddd14d01eaeb377e77bf36a0f3be`。上一版“导出剪贴板版”是局部开发版，不是最终成品。
- 不要删除真实 `runtime/2.3.0/WPE.db`。所有测试用隔离目录；不得用用户真实数据作破坏性测试。

## 总体状态与最终验收

基线已有：原 Vue/WebView2 宿主、公共协议与队列、SQLite 8/20 表、四个列表及编辑器、明文发送集/仓储条目导入导出、剪贴板。基线 64 数据 + 9 宿主接口注册（共 217 原 RPC），仍有 144 未注册；注册不等于完成。

以下仍未全部实现，最终必须按原源码逐项补齐和验收：剩余数据表/配置/列表、代理运行与连接、注入与 Hook、真实抓包数据链路、发送/机器人/滤镜执行、远程相关能力及原版其余 RPC。不要隐藏不支持，不要伪造结果，不要把模拟器、固定路径测试或未执行的代码描述成真实功能验收。

最终交付门槛：完整源码/RPC/事件/行为对照清单，原 Vue 及资源哈希保持，功能主路径和重要错误路径验证，x64/Win32 公共库同测，原程序加载器双向格式对照，独立代码审查，干净源码重建和打包后实际运行，输出真实证据。阶段推进暂不反复发布 ZIP；最终才交付程序、源码、构建说明、对照与测试结果。

## 当前在做：父列表/五组备份/原版加密

生产新增 `xml_crypto.*`（Windows CNG）、`xml_document.*`（XmlLite）、`config_xml.*`、`config_xml_fields.h`、`data_files.cpp`，修改数据服务、编辑器 XML 和宿主文件流程。代码尚处开发验证中，未作为新版成品交付。

范围：`.fp/.sp/.rp/.whp` 全部或选中导出、追加导入；`.sc/.whs` 加密互通；`.sb` 先支持 SystemConfig+四个列表。原版备份共 14 项，其余 9 项仍须后续实现。当前对不支持分组必须整次失败，不得输出缺项备份或静默丢数据。

原版格式关键点：

- 系统 ACP 编码密码（本机原 .NET Encoding.Default=936），MD5 后取 digest[4..11] 的大写十六进制 16 字符作为 ASCII 密钥；AES-128 CBC PKCS7，IV=key，原始密文无头。这只是旧格式兼容，不是现代安全设计。
- XML UTF8 BOM、standalone=yes、CRLF、2 空格缩进；空字符串与 null 标签形态不同。普通列表导入追加，重复 GUID 重建；备份只替换存在的分组，缺失保持，空分组清空。
- Filter 的 Guid.Empty 能导入，其余三种父列表跳过；Send 导入/导出 SystemSocket 始终 false，父列表导出不带 IPFrom；机器人导入不套编辑器内容校验；仓储内容为十六进制且受容量上限裁剪。
- 父列表布尔 True/False，系统配置布尔 true/false。已有 65 个系统字段、27 个滤镜字段按原文件顺序提取。
- Vue 导出密码框成功返回 null（包括 Esc）表示“不加密”；桥接失败/超时/导航取消绝不能降级成明文导出。加密导出不能先写明文临时文件。
- 密码验证只能用当前原生文件选择器授予的 token 与路径；不得开放浏览器任意路径读取。导入内容选择后在 worker 缓存，验证/应用针对同一字节内容；取消/关闭/队列满释放计划。
- 普通导出冻结成员及顺序，但原 PacketInfo/父对象字段的浅拷贝别名要保留；备份在密码框完成后读取实时状态。备份整批替换不应改旧导出快照的对象身份。

当前实现接线：

- DataService 新增 10 数据接口（4 组导入导出+backup 两个），宿主新增 verifyEncryptPassword。已从源码重新核对并生成清单：74+10=84 个注册，仍有 133 未注册；不是完成率。
- 宿主 BeginImport / 密码对话框已接，原 Vue 未改；新 UI 自测已经真实验证加密导出→原按钮清空→错误密码重试→加密导入，含伪造路径及grant消费后拒绝。
- `DataWorker::ForgetImportPlan` 新增无队列容量限制的内部清理入口。
- 批量备份数据库事务化：非法/未支持分组在改库前拒绝。比原版出错时可能部分应用更严格，此差异要明确记录。

## 构建、证据与待办

开发构建目录：`E:\codex\2026-09-17\bi-a\work\config-dev-x64`。当前新增专项 **198 项通过**。原 C# 35 密文向量逐字节双向通过，完整四父列表/五组备份、别名、取消、事务故障、XML namespace/根名/混合文本/尾分隔符回归已收入测试。

原测试 oracle：`tests/data_oracle/ConfigFileOracle.cs`，Program 的 `config-files input output` 与 `verify-encrypted file password`。原程序集黄金使用 `work/config-wire/goldens-2`（goldens-1 是首次 null 系统字段研究结果，不再作为最终预期），复制到了 `tests/fixtures/config-files/original`；独立 reviewer 的边界黄金进入 `edges`。测试程序为 `work/config-wire/oracle/WpeDataOracle.exe`。75+ 固定原产物/契约哈希见 contracts/data-oracle.json；字段生成器已参数化保存在 tests/data_oracle/extract_xml_fields.py。

已完成独立 Standards/Spec 两轴审查并修复，见 REVIEW-config-wiring.md。最后根名修复经 Spec 独立重跑 198 项通过。初次fresh双架构 run-0a1f63ecdd214bc58845e37b7c34d853（work/config-final）均 10/10，协议74684/断言76618/600反向封包/6416队列全部通过，原Vue含加密与重启通过；该次早于最后根名修复。最终源码 work/config-final2/run-09b5e6db4cfe42749d3b8d8229eb9502 已完成全新双架构：各10/10、专项198、原Vue加密/密码重试/授权限制/重启通过，555资源/70Vue保持。原 C# 另外成功解密两架构各5个实际导出文件及UI生成的加密.sc。最终manifest源文件哈希逐一与当前工作区核对通过。证据已归档 evidence/config-*；仍只是局部功能通过，不是最终完整产品。

当前无阶段 ZIP 交付。先完成本轮最终记录与提交，再直接推进下一组功能，不向用户索要“继续”：

1. 本轮 final2、证据归档、原 C# 实际反向解密和双轴审查均已完成，不要重复重跑同一阶段取代新开发。只有后续代码改变相关行为才进行对应回归。
2. 本切片完成提交后以 git log 的最新记录为准，保持源码干净可恢复；未生成新阶段ZIP，用户要求完整功能后才交付。若看到尚未提交改动先核对，不丢弃。
3. 下一实际开发优先：其余 12 张数据库表及相关原 RPC、代理设置/账号/黑白名单/映射/注入设置/自动入库/服务端两类规则，补齐目前被拒绝的九个 .sb 分组。按原源码建表和Load/Save/Feed对照，保留Vue，不能只注册处理器。
4. 接着按文档里程碑推进真实 IPC、注入/Hook/封包链路、滤镜引擎、发送/机器人执行与代理/远程能力；原页面每个入口和回传数据均须核对。contracts/rpc-status.json 与 217接口 CSV 随实现更新。
5. 四父列表/备份全部可见按钮组合、Windows 选择框、TopMost 根因、完整语言/系统主题跟随仍未全部验收；最终打包前必须回到完整行为清单补齐。

工具：CMake4.4.2，VS2022 BuildTools 14.44，.NET Framework4.8 + SDK8；Python `C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`。

正式构建用 build-test.ps1，每次 fresh build root，保持路径短（260 字符风险）。旧 `work/exchange-wire/prepare_package.py` / `package_release.py` 可参考但不可未经更新直接运行。源码 ZIP 比对以从 ZIP 干净构建生成的 manifest 为准，不能和会自动换行的工作区盲比。

已知旧边界：TopMost 隐藏进程探针历史不稳定，不宣称根因修复；OS 剪贴板测试在 private window station，UI 自测用隔离内存；WebView2 Runtime153 为外部运行环境。最终报告按实际测试而非历史成功推断。
