# WPE C++ 无人值守接续

更新：2026-09-18。本文件是开发状态记录，不是已完成交付声明。

## 用户要求与运行方式

- 用户要求完整实现原版功能与体验，保留全部原 Vue，完成检查、修复、优化后再交付；不再等待用户回复“继续”。
- 当前任务已通过应用工具创建 heartbeat 自动接续 `wpe`，名称“WPE 原版功能无人值守移植”，ACTIVE，每 10 分钟接续检查。不是另外的新任务。常规推进保持安静，只在全部验收完成或必须用户介入时通知。
- 每轮实际推进代码与测试，记录下一步；不要反复仅审计、打包局部版本或者空报进度。不能承诺一夜完成，不能把接口注册当成功能完成。
- 电脑、Codex、网络和账户额度是运行条件。自动任务存在不等于可以在关机后执行。

## 路径与输入

- 项目：`E:\codex\2026-09-17\bi-a\outputs\WPE64-Cpp`。
- 中间文件与构建：`E:\codex\2026-09-17\bi-a\work`；交付只放 `outputs`。
- 原 ZIP：`C:\Users\Administrator\Downloads\WinsockPacketEditor-master.zip`，SHA256 `1F524B84621F064ACD91C3F9D764D1ADDF19AC42019E7FCEDE11BF55EA536113`。
- 更新文档：`docs/用户提供-重写工程文档-更新版.md`，以 `contracts/active-spec.json` 为准。
- 原源码：`E:\codex\2026-09-17\bi-a\work\source-audit\WinsockPacketEditor-master\WinsockPacketEditor`，重点 `Operate.cs`、`ShellForm.cs`。
- 原业务程序集：`E:\codex\2026-09-17\bi-a\work\baseline-build\WinsockPacketEditor-master\WinsockPacketEditor\bin\Release\WinsockPacketEditor.exe`，SHA256 `76C33B88188F7F921AE5B21117533FF1D3F1861A04A723B8087DE42B17E5FEED`。只用于测试，不是产品依赖。
- 当前可恢复开发切片以 `git log -1` 为准；历史基线 `61022ed32850ddd14d01eaeb377e77bf36a0f3be`。上一版“导出剪贴板版”是局部开发版，不是最终成品。
- 不要删除真实 `runtime/2.3.0/WPE.db`。所有测试用隔离目录；不得用用户真实数据作破坏性测试。

## 总体状态与最终验收

当前已有：原 Vue/WebView2 宿主、公共协议与队列、三通道 Windows 命名管道传输、外壳/目标会话生命周期及目标无头协议核心、x64/Win32 同位数进程注入与挂起启动资源管理、生产原生目标 DLL 的无加载器锁启动入口和真实跨进程 v4 会话、MinHook 13 个 WinSock 签名（保持原版 x64 跳过 WSARecvEx）与真实 TCP/UDP 抓包回传、SQLite 20/20 表、四个父列表及编辑器、自动入库规则、白/黑名单、代理账号、双向映射、WPC HTTP 服务端/规则/通知发布、明文/原 AES 配置文件、剪贴板。当前注册数量和未注册数量以 `contracts/rpc-status.json` 为准（共 217 原 RPC），注册不等于完成。

以下仍未全部实现，最终必须逐项补齐和验收：私有 SOCKS WPC 控制协议及其余范围内 RPC；P4-1 的滤镜→WareHouse、P4-2 的滤镜→Send、P4-3 的 SOCKS5 TCP CONNECT、P4-4 的 HTTP/HTTPS 正向代理、P5-1 的 HTTP 映射、P5-2 的 SOCKS5 UDP ASSOCIATE 和 P5-3 的 WPC HTTP 节点/规则/公告发布已完成真实执行链与回归。x86 helper 和原 Vue 注入入口已完成代码接线，但真实用户目标窗口的权限/提前退出/StartHook 失败人工验收仍未完成。目标侧单包 `SendPacket` / `GetSocketInfo` 与普通 Winsock 生命周期 registry 已完成；同步 Winsock 路径已执行滤镜修改/拦截及 Filter→Filter 联动；非空 overlapped WSA 完成期仍未实现。机器人运行执行器和 Filter→Robot 已按用户要求移出交付范围，但原机器人数据、文件格式、列表和指令编辑兼容继续保留。不要隐藏不支持，不要伪造结果，不要把模拟器、固定路径测试或未执行的代码描述成真实功能验收。

最终交付门槛：完整源码/RPC/事件/行为对照清单，原 Vue 及资源哈希保持，功能主路径和重要错误路径验证，x64/Win32 公共库同测，原程序加载器双向格式对照，独立代码审查，干净源码重建和打包后实际运行，输出真实证据。阶段推进暂不反复发布 ZIP；最终才交付程序、源码、构建说明、对照与测试结果。

## 已完成切片：全部数据表/十四组备份/原版加密/配置与列表

生产新增 `xml_crypto.*`（Windows CNG）、`xml_document.*`（XmlLite）、`config_xml.*`、`config_xml_fields.h`、`data_files.cpp`，修改数据服务、编辑器 XML 和宿主文件流程。代码尚处开发验证中，未作为新版成品交付。

范围：`.fp/.sp/.rp/.whp` 全部或选中导出、追加导入；`.sc/.whs/.wl/.bl/.pa/.pas/.pml/.pmr` 明文及原 AES 互通；`.sb` 已支持原版全部 14 组：SystemConfig、ProxyMode、ProxyAccountList、WhiteList、BlackList、InjectMode、AutoStores、ProxyMapping、WPCServer、WPCNotice 和四个父列表。非法分组必须整次失败，不得输出缺项备份或静默丢数据。

原版格式关键点：

- 系统 ACP 编码密码（本机原 .NET Encoding.Default=936），MD5 后取 digest[4..11] 的大写十六进制 16 字符作为 ASCII 密钥；AES-128 CBC PKCS7，IV=key，原始密文无头。这只是旧格式兼容，不是现代安全设计。
- XML UTF8 BOM、standalone=yes、CRLF、2 空格缩进；空字符串与 null 标签形态不同。普通列表导入追加，重复 GUID 重建；备份只替换存在的分组，缺失保持，空分组清空。
- Filter 的 Guid.Empty 能导入，其余三种父列表跳过；Send 导入/导出 SystemSocket 始终 false，父列表导出不带 IPFrom；机器人导入不套编辑器内容校验；仓储内容为十六进制且受容量上限裁剪。
- 父列表布尔 True/False，系统配置布尔 true/false。已有 65 个系统字段、27 个滤镜字段按原文件顺序提取。
- Vue 导出密码框成功返回 null（包括 Esc）表示“不加密”；桥接失败/超时/导航取消绝不能降级成明文导出。加密导出不能先写明文临时文件。
- 密码验证只能用当前原生文件选择器授予的 token 与路径；不得开放浏览器任意路径读取。导入内容选择后在 worker 缓存，验证/应用针对同一字节内容；取消/关闭/队列满释放计划。
- 普通导出冻结成员及顺序，但原 PacketInfo/父对象字段的浅拷贝别名要保留；备份在密码框完成后读取实时状态。备份整批替换不应改旧导出快照的对象身份。

当前实现接线：

- DataService 已接通代理账号 15 个、自动入库 6 个、映射 7 个、WPC 服务端/规则/通知 13 个原 RPC；连同既有方法为 126 个数据 + 10 个宿主注册，仍有 81 未注册；这不是完成率。批量生成按原时间/前缀规则与边界产生草稿，保存保持区分大小写的重名跳过、原密码映射、事务落库和单次 `feed:append`，导出保持原 `.xls` 实际 ACP 制表文本格式且不弹 XML 加密密码框。
- `ProxyMode`、`InjectMode` 使用原建表 SQL、原 XML 字段顺序及 `Enable_Auth`/`EnableAuth` 映射，真实事务读写；代理设置枚举本机地址并按原 32/64 位内存规则计算连接上限。未启动监听、未向目标进程推 Hook 标志、未执行防火墙。
- 宿主 BeginImport / 密码对话框已接，原 Vue 未改；新 UI 自测已经真实验证加密导出→原按钮清空→错误密码重试→加密导入，含伪造路径及grant消费后拒绝。
- `DataWorker::ForgetImportPlan` 新增无队列容量限制的内部清理入口。
- 批量备份数据库事务化：非法/未支持分组在改库前拒绝。比原版出错时可能部分应用更严格，此差异要明确记录。
- WhiteList/BlackList 通过原表、原 feed 15/16 和原 Vue 页面接通；新增/编辑/删除/清空、`.wl/.bl` 导入导出与十组备份均持久化。IP 归属地仍为空，实际防火墙判定和命中计数未接。
- ProxyAccount/ProxyAccountIPInfo 原表、feed 5、原 Vue 增改删清/启用/限制与到期调整/登录记录及批量生成/保存/导出已接；原密码映射、`.pa` 明文/AES、选中/全部导出和备份替换已接。真实代理认证、在线状态和 IP 归属地未接。
- AutoStores 原表、运行期 AID、feed 12 与原 Vue 自动入库页面已接；规则新增/编辑/删除/启用/排序、运行期总开关、容量持久化、`.pas` 明文/AES 及备份均已接。总开关按原版重启关闭，规则保持；精确重复包头保留原版“本次运行可见、落库跳过”的异常语义。P4-1 已把滤镜 WareHouse 命中后的真实封包写入仓库执行链接上；P4-3 已接入 SOCKS5 TCP CONNECT，P4-4 已接入 HTTP/HTTPS 正向代理与 CONNECT 隧道，P5-1 已接入 HTTP 映射，P5-2 已接入 SOCKS5 UDP ASSOCIATE；WPC 执行链仍未完成。
- ProxyMapLocal/ProxyMapRemote 原表、运行期映射 ID、feed 13/14 与原 Vue `映射设置` 已接；双向规则的新增/编辑/删除/清空/启用/排序、映射总开关、`.pml/.pmr` 明文/AES 追加导入导出及 `proxyMapping` 备份均持久化。页面当前只支持原版 HTTP 类型；P5-1 已把启用规则接入 HTTP 代理的本地文件响应和远程目标重写，CONNECT 加密隧道不匹配路径。
- ServerInfo/ServerRuleInfo/NoticeInfo 原表、父子关系、feed 17/18 与原 Vue `WPC 配置` 已接；服务端、24 类规则、分号规则展开、通知的增改删清/启用/排序以及 `wpcServer`/`wpcNotice` 备份均持久化。P5-3 已增加真实 WPC HTTP 发布服务、运行期快照更新、Basic 健康状态和停止收尾；私有 SOCKS 控制连接、设备注册令牌、长连接重连和 WPC-only 代理流量仍未接。
- `src/common/ipc_pipe.*` 已实现真实 Windows NPFS 三通道传输：外壳创建 ctl 双向及 pkt/evt 单向服务端，目标侧限时连接，字节流使用原 `IpcFrame` 分帧；会话名同时严格接受原 `Guid.ToString("N")` 的 32 位十六进制和诊断用 36 位 `D` 形态，拒绝路径逃逸。管道拒绝远程客户端，显式 DACL 仅给当前用户 FullControl，控制帧 1 MiB/封包帧 16 MiB 上限在 I/O 前执行。服务端和客户端均改为 overlapped I/O，服务端 Accept 可超时并调用 `CancelIoEx` 收尾，接收线程可先取消、等待退出、再安全关闭句柄。已在 x64/Win32 对真实 OS 管道验证 Hello 应答、PID/位数、1 MiB 跨缓冲封包、事件、EOF、缺失服务端/Accept 超时、阻塞读取取消和 ACL 内容。该层不包含注入、Hook 或封包来源，不能单独称 M1/M2 完成。
- `src/common/ipc_session.*` 已在真实三管道之上实现生产会话层：外壳三管道总时限 Accept、Hello v4/版本拒绝、目标 PID/位数、控制请求互斥一问一答、pkt/evt 独立接收线程、1 秒心跳和断开状态；目标侧所有命令先刷新心跳、3 秒 watchdog、错误应答、pkt 写互斥，以及原版 8192 条/16 MiB 的 evt 有界丢旧队列和唯一事件写线程。事件生产者不再同步写管道；写超时后整条会话失败，禁止在可能已有半帧的流上继续写。Detach 仍先回 Ok/Flush、等待 50 ms 再退出。超时回调是卸 Hook/停执行器的真实切口。StartHook、五类 SetConfig、SendPacket 和 GetSocketInfo 已接真实生产控制器；发送列表执行器命令仍未实现，机器人运行命令按用户要求不实现。
- `src/target/headless_core.*` 已实现目标无头协议核心：五类 `SetConfig` 快照全部按 v4 字段顺序解析并原子替换，滤镜/发送/机器人运行计数按 GUID 迁移；`StartHook`/`StopHook`、四组 `ResetStats` 掩码、Hello 时被动 WinSock 探测、`HookState`/`Fatal` 以及完整 1 Hz `Stats` 字段顺序已接入真实 `TargetIpcSession`。目标特有装钩动作隔离在 `IHookController`；HookFlags、极速模式和 11 项原子封包统计已接真实生产控制器，`ResetStats(PacketCounters)` 由统计所有者执行。会话同时修复两个实测生命周期竞态：长命令处理期间 watchdog 不再误判超时；Detach 回复后心跳不能抢控制锁再向已退出目标发送 Ping。pkt/evt 写均为 500 ms 有界等待，外壳消失时不会把目标收尾永久钉死。
- `src/shell/process_injector.*` 已实现原生同位数注入基础：优先 `IsWow64Process2` 检测目标位数，拒绝跨位数；按目标 `kernel32!LoadLibraryW` 的模块内偏移构造远程入口，使用有界远程线程等待并通过目标模块表确认 DLL 实际加载；进程刚恢复、模块快照暂不可读时有界重试。失败路径回收句柄和远程内存，超时时避免释放仍可能被远程线程读取的路径。另有 `CreateProcessW(CREATE_SUSPENDED)` 启动对象，未恢复即析构会终止并等待子进程，显式 `Resume` 后转为普通句柄生命周期。`InjectAndStart` 会把固定布局、版本化、无指针的 bootstrap 写入目标并调用未修饰导出 `WpeStart`。新增 `src/shell/x86_helper_main.cpp`：只允许 Win32 构建，x64 外壳通过命名事件调度运行中/挂起 x86 目标注入和恢复。x64/Win32 均对真实独立进程验证，不是只测接口注册。
- `src/target/dllmain.cpp` 与 `injected_runtime.*` 已生成真正的 x64/Win32 `wpe64-hook.dll`：`DllMain` 不做初始化，远程 `WpeStart` 只校验/复制 bootstrap 并启动专用工作线程；目标线程连接真实三管道、运行 `TargetIpcSession + HeadlessCore + WinsockHookController`，支持 Hello、五类配置、统计、错误应答、心跳、自收尾和 Detach。没有外壳管道时按给定时限静默收尾，重复入口在当前会话活跃时拒绝；DLL 只导出未修饰 `WpeStart`。官方 MinHook 1.3.4 源码已静态纳入 `third_party/minhook`；`HookManager` 只停用/移除自身目标并对进程全局 MinHook 运行时引用计数，失败资源保留供重试，不会误拆宿主已有钩子。`winsock_hook.*` 已实现原版 13 个抓包签名、普通 socket 生命周期元数据 hook、有界不可变字节复制、64 MiB/65536 项丢旧环、专用地址/编码/pkt 写线程和丢包事件；卸钩先关闭新 detour 的 owner 获取，再等待已进入调用退出，阻塞 recv 超时时保留 trampoline/owner，由后台安全退休。同步滤镜、SendPacket 与 GetSocketInfo 已接入；主动重放使用 TLS 旁路后直达 trampoline，不会再次抓包或滤镜。非空 overlapped WSA 完成期滤镜和发送列表执行器尚未接入。机器人运行执行器不再列为待实现项。

## 构建、证据与待办

上一轮 IP 名单全新验证：`E:\codex\2026-09-17\bi-a\work\iprules-final2\run-08d2da51405b4db3be7c0bf1ca32a2ad`。x64/Win32 各 10/10，配置专项 **274 项通过**、数据 444、编辑 229（184 条原调用）、导出 41。原 C# 49 密文向量逐字节双向通过，完整四父列表/九组备份、旧库补列、本机地址格式、IP 范围/日期、别名、取消、事务故障、XML namespace/根名/混合文本/尾分隔符回归已收入测试。两架构仍通过 74684 协议向量、76618 断言、600 反向封包和 6416 队列向量；555 资源/70 Vue 保持。manifest SHA256 `1F2B1A1F2D1621AF0583971FC628AC9628D1FC61257208985CBED7274879BA8D`，总结果、两架构、原 Vue/WebView2 宿主与重启均为 passed。首次宿主运行真实从原 Vue 设置菜单新增白名单 IP，重启后再次在原页面确认持久化。

原 C# 的 `verify-settings-db` 与 `verify-ip-rules-db` 只作测试：前者从原生 SQLite 加载 ProxyMode/InjectMode 并与黄金逐字节相同；后者从本轮原生 SQLite 实际加载 WhiteList/BlackList。原程序另已成功解密本轮原生导出的中文密码 `.wl`。

原测试 oracle：`tests/data_oracle/ConfigFileOracle.cs`，Program 的 `config-files input output` 与 `verify-encrypted file password`。原程序集黄金使用 `work/config-wire/goldens-2`（goldens-1 是首次 null 系统字段研究结果，不再作为最终预期），复制到了 `tests/fixtures/config-files/original`；独立 reviewer 的边界黄金进入 `edges`。测试程序为 `work/config-wire/oracle/WpeDataOracle.exe`。75+ 固定原产物/契约哈希见 contracts/data-oracle.json；字段生成器已参数化保存在 tests/data_oracle/extract_xml_fields.py。

已完成独立 Standards/Spec 两轴审查并修复，见 REVIEW-config-wiring.md。最后根名修复经 Spec 独立重跑 198 项通过。初次fresh双架构 run-0a1f63ecdd214bc58845e37b7c34d853（work/config-final）均 10/10，协议74684/断言76618/600反向封包/6416队列全部通过，原Vue含加密与重启通过；该次早于最后根名修复。最终源码 work/config-final2/run-09b5e6db4cfe42749d3b8d8229eb9502 已完成全新双架构：各10/10、专项198、原Vue加密/密码重试/授权限制/重启通过，555资源/70Vue保持。原 C# 另外成功解密两架构各5个实际导出文件及UI生成的加密.sc。最终manifest源文件哈希逐一与当前工作区核对通过。证据已归档 evidence/config-*；仍只是局部功能通过，不是最终完整产品。

当前无阶段 ZIP 交付。先完成本轮最终记录与提交，再直接推进下一组功能，不向用户索要“继续”：

1. 本轮 IP 名单、原 C# 实际反向解密、原 Vue 首次运行/重启与全新双架构回归均已完成，不要重复重跑同一阶段取代新开发。只有后续代码改变相关行为才进行对应回归。
2. 本切片完成提交后以 git log 的最新记录为准，保持源码干净可恢复；未生成新阶段ZIP，用户要求完整功能后才交付。若看到尚未提交改动先核对，不丢弃。
3. 目标无头协议核心、同位数注入器、生产目标 DLL 的 bootstrap/真实跨进程会话、MinHook 封装、原始抓包、同步滤镜执行链以及 P1 `SendPacket` / `GetSocketInfo` /普通 socket registry 已完成。P2 发送执行器已完成；P3-1 挂起编排和 P3-2 x86 helper/原 Vue 入口代码接线已完成。下一实际开发是原 Vue 在真实目标窗口上的权限、提前退出、StartHook 失败和断开错误矩阵人工验收，再将现有自动入库、映射、代理和 WPC 配置接到真实执行链路。机器人运行执行器及 Filter→Robot 不实现。
4. 原页面每个入口、状态事件和回传数据均须对照原源码核验；`contracts/rpc-status.json` 与 217 接口 CSV 随实现更新。数据库 20/20 表和 `.sb` 14/14 分组已完成，不要重复把它们列为待办。
5. 四父列表/备份全部可见按钮组合、Windows 选择框、TopMost 根因、完整语言/系统主题跟随仍未全部验收；最终打包前必须回到完整行为清单补齐。

工具：CMake4.4.2，VS2022 BuildTools 14.44，.NET Framework4.8 + SDK8；Python `C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`。

正式构建用 build-test.ps1，每次 fresh build root，保持路径短（260 字符风险）。旧 `work/exchange-wire/prepare_package.py` / `package_release.py` 可参考但不可未经更新直接运行。源码 ZIP 比对以从 ZIP 干净构建生成的 manifest 为准，不能和会自动换行的工作区盲比。

已知旧边界：TopMost 隐藏进程探针历史不稳定，不宣称根因修复；OS 剪贴板测试在 private window station，UI 自测用隔离内存；WebView2 Runtime153 为外部运行环境。最终报告按实际测试而非历史成功推断。

最新代理账号切片 `run-7f427f15649e4d14b2d983f2bceb992d`：从当前源码全新构建，x64/Win32 各 10/10 CTest；配置专项 317、数据 460、编辑 229（184 条原调用）、导出 41 全部通过。两架构均通过 74684 协议向量、76618 断言、600 反向封包和 6416 队列状态向量；555 资源/70 Vue 哈希保持。原 Vue 首次运行真实新增代理账号，重启后 feed 5、列表 DOM 和明文密码再次核验通过；宿主与重启均 passed。未修改的原 C# 从两架构原生 SQLite 读取 ProxyAccount/ProxyAccountIPInfo 后重写 `.pa`，SHA256 均为 `B20620478C4FF345F707B4AEF095F3F649CD1406C4D7AD5824C7D87B89AAC57C`，并真实解密两架构原生加密 `.pa`。manifest SHA256 `12F73765855B9FB4967BF13B4857DFBE81FC3873B7AFC7C4D5C5C2302E63F640`；证据目录：`E:\codex\2026-09-17\bi-a\work\accounts-final\run-7f427f15649e4d14b2d983f2bceb992d`。这仍是账号数据/文件切片，不是代理认证运行引擎或完整产品验收。

最新账号批量切片 `run-1c8ca4ffc31e43b3af228d7599320e92`：从当前源码全新构建，x64/Win32 各 10/10 CTest；配置专项 325、数据 320、编辑 229（184 条原调用）、导出 41 全部通过。两架构均通过 74684 协议向量、76618 断言、600 反向封包和 6416 队列状态向量；555 资源/70 Vue 哈希保持。原 Vue 首次运行实际打开 `Batch Create`、生成两条、通过无需加密框的 `.xls` 文件流程导出、保存并收到单次追加，重启后两个批量账号与单账号密码均再次核验，宿主和重启报告 `batchAccountRoundTrip=true`。原程序集反射调用私有 `SaveBatchAccountsToExcel` 产生固定 ACP936 黄金 `batch-accounts.xls`，SHA256 `F768E3F92A6A5EDD7349171796FAA1992CDD2E473042D6DD9235EE470006C982`，本机 C++ 输出逐字节一致；manifest SHA256 `8A96C48A29857B8201CC55BB0772FAA82090E13BC9634C1CDD1C5FF6ECC79B66`。证据目录：`E:\codex\2026-09-17\bi-a\work\batch-final2\run-1c8ca4ffc31e43b3af228d7599320e92`。这仍是账号批量数据/文件切片，不是代理认证运行引擎或完整产品验收。

最新自动入库切片 `run-a510eda4e503429885018ca3c1a8e858`：从当前源码全新构建，x64/Win32 各 10/10 CTest；配置专项 335、数据 331、编辑 229（184 条原调用）、导出 41 全部通过。两架构均通过 74684 协议向量、76618 断言、600 反向封包和 6416 队列状态向量；555 资源/70 Vue 哈希保持。原 Vue 首次运行实际打开仓库页的 `Auto Store`、通过 `Auto-Store Rule` 新增并启用 `16 03 01` 规则、保存运行期总开关；重启后规则与启用状态仍在、总开关按原版恢复关闭，宿主与重启均报告 `autoStoresRoundTrip=true`。专项另验证 `.pas` 原 XML 字节布局、原 AES 双向、追加导入、十一组 `.sb` 备份替换、SQLite/AID/feed 12、校验/排序/容量边界。manifest SHA256 `DC8359885E28CCE0EFEF5E49E61FC4D08D48667E9B789043EEA8357524B20344`；证据目录：`E:\codex\2026-09-17\bi-a\work\autostores-final3\run-a510eda4e503429885018ca3c1a8e858`。这仍是自动入库规则数据与配置文件切片；真实封包匹配和自动写入仓库尚未接执行链路，不是完整产品验收。

最新全部配置列表切片 `run-7b15ef5d6df044c8b2d8c95d1d4c896f`：从当前源码全新构建，x64/Win32 各 10/10 CTest；数据 346、配置专项 349、编辑 229（184 条原调用）、导出 41 全部通过。两架构均通过 74684 原 C# 协议向量、76618 断言、600 反向封包和 6416 队列状态向量；555 原资源/70 Vue 文件保持。原 Vue 首次运行真实打开 `映射设置` 并核验映射行，再进入 `WPC 配置` 核验服务端、两条规则和通知；重启后再次从原页面确认持久化，宿主与重启均报告 `configurationListsRoundTrip=true`。专项另验证 20/20 SQLite 表、feed 13/14/17/18、`.pml/.pmr` 原 XML 形态和 AES 往返，以及全部 14 组 `.sb` 导出、清空、导入恢复。manifest SHA256 `24FC240EC8C2712021F6AE13EC01B20DCC751F9FC40441E72DD3FDB3A99B68B2`；证据目录：`E:\codex\2026-09-17\bi-a\work\configlists-final2\run-7b15ef5d6df044c8b2d8c95d1d4c896f`。这仍是配置、持久化和原 Vue 接线验收；实际 HTTP 映射执行、远程 WPC 服务、注入/Hook/抓包和封包执行尚未接通，产品未完成。

最新命名管道传输切片 `run-6b2ce17d4d194f13bbdf201115ccfae8`：从当前源码全新构建，x64/Win32 各 11/11 CTest；新增两架构各 25 项真实 NPFS 检查，覆盖当前用户唯一 DACL ACE、ctl Hello 往返、pkt/evt 单向流、1 MiB 跨缓冲负载、EOF、帧上限、非法会话名和连接超时。既有数据 346、配置 349、编辑 229（184 条原调用）、导出 41 全部保持；两架构仍通过 74684 原 C# 协议向量、76618 断言、600 反向封包和 6416 队列状态向量，555 原资源/70 Vue 文件保持，原 Vue/WebView2 首次与重启宿主均 passed。manifest SHA256 `41FF07662F4B7F21D6CAB381021F0D07124F6265CDA8BB54F826693ABCFC6CBD`；证据目录：`E:\codex\2026-09-17\bi-a\work\pipe-final\run-6b2ce17d4d194f13bbdf201115ccfae8`。此切片证明传输层真实运行，不代表注入、目标端协议循环、心跳、Hook 或抓包已经完成。

最新管道兼容与取消切片 `run-8272a88cde064fd1bc1071079bb27805`：修复前一切片错误只接受带横线 GUID、与原 `ShellLink` 实际 32 位 N 格式不兼容的问题，并将两端改为可取消的 overlapped I/O。全新 x64/Win32 各 11/11 CTest；两架构各 28 项真实 NPFS 检查新增原 N/D 两种会话名、服务端 Accept 超时取消和阻塞读取取消，所有句柄在等待线程退出后关闭。既有数据 346、配置 349、编辑 229（184 条原调用）、导出 41、74684 原 C# 协议向量、76618 断言、600 反向封包、6416 队列状态向量、555 原资源/70 Vue，以及原 Vue/WebView2 首次与重启宿主全部保持通过。manifest SHA256 `8CBA7D5239CEBA9F6405FE05CD9C30E6735AE05B8A7B57635FB846201E85751C`；证据目录：`E:\codex\2026-09-17\bi-a\work\pipe-lifecycle-final\run-8272a88cde064fd1bc1071079bb27805`。下一轮从此外壳/目标会话层接续，不能把管道单测当成端到端注入完成。

最新 IPC 会话切片 `run-8f1de684412742e3bc992abd17bc183a`：从当前源码全新构建，x64/Win32 各 12/12 CTest；新增两架构各 37 项真实会话检查，覆盖 Hello v4、版本不符拒绝、PID/位数、八线程控制调用串行与应答不串线、pkt/evt 接收、定时 Ping 保活、任意普通命令刷新心跳、命令停止后目标 watchdog 超时一次性清理、Error 应答以及 Detach 已应答后退出；外壳心跳等待可被 Stop 立即唤醒，不再最多拖延一个周期。管道传输各 28 项仍通过；既有数据 346、配置 349、编辑 229（184 条原调用）、导出 41、74684 原 C# 协议向量、76618 断言、600 反向封包、6416 队列状态向量、555 原资源/70 Vue 和原 Vue/WebView2 首次/重启宿主全部保持。manifest SHA256 `14BD0AF0238280196E6A142B935B8819FEC5F22F7BF319AD0C7CB7700F5D587C`；证据目录：`E:\codex\2026-09-17\bi-a\work\session-final2\run-8f1de684412742e3bc992abd17bc183a`。这验证的是同进程内两端经真实 OS 管道跑完整会话生命周期；尚未有注入进程、Hook 或真实 Winsock 封包，不是端到端抓包验收。

最新目标无头核心切片 `run-c0cce8c3ffa8484b99fd76fa716005c4`：从当前源码全新构建，x64/Win32 各 13/13 CTest；两架构各 87 项目标核心检查覆盖 HookFlags/Filters/Runtime/Sends/Robots 五类完整快照、非法尾字段不部分改状态、GUID 计数迁移、发送循环值归一、Stats 精确字段、四组 ResetStats 隔离、Hello/Start/Detach HookState、Fatal、周期统计和退出卸 Hook；会话各 47 项新增长命令不误超时及 12 轮 1 ms 心跳/Detach 控制锁竞态。另连续运行目标核心 50 次通过以复现并修复退出卡死，记录为 `evidence/headless-stress-50.log`；双架构 CTest 和 manifest 同步归档为 `evidence/headless-*`。既有管道 28、数据 346、配置 349、编辑 229（184 条原调用）、导出 41 保持；两架构仍通过 74684 原 C# 协议向量、76618 断言、600 反向封包、6416 队列向量，555 原资源/70 Vue 以及原 Vue/WebView2 首次和重启宿主均通过。manifest SHA256 `231224136C42D1227770CE05C89B2879426C856626152711C43614CF11B15F9D`；证据目录：`E:\codex\2026-09-17\bi-a\work\headless-final2\run-c0cce8c3ffa8484b99fd76fa716005c4`。`IHookController` 当前仅用有状态测试替身验收协议和生命周期；真实注入 DLL、MinHook、13 个 Winsock 钩子与抓包仍未实现，因此产品仍未完成。

最新进程注入基础切片 `run-1a80aac4c216441faf4aedb070afa2a2`：从当前源码全新构建，x64/Win32 各 14/14 CTest；新增两架构各 13 项注入检查，使用真实独立目标进程和测试 DLL 验证当前进程位数识别、运行中同位数目标的远程 `LoadLibraryW`、模块表加载确认、非法 PID/非 DLL/零超时/缺失可执行文件拒绝、`CREATE_SUSPENDED` 显式恢复以及未恢复对象析构时终止等待。双架构构建完成后又实际执行 x64→Win32、Win32→x64 两个交叉目标测试，均在远程执行前拒绝。既有目标核心 87、会话 47、管道 28、数据 346、配置 349、编辑 229（184 条原调用）、导出 41 保持；两架构仍通过 74684 原 C# 协议向量、76618 断言、600 反向封包、6416 队列向量，555 原资源/70 Vue 以及原 Vue/WebView2 首次和重启宿主均通过。manifest SHA256 `804F7870C8FFA14139AC5B8507E86A4C091CBABDAE4C4EE29E3B804DEBFB8899`；完整证据目录：`E:\codex\2026-09-17\bi-a\work\injector-final2\run-1a80aac4c216441faf4aedb070afa2a2`，轻量证据归档为 `evidence/injector-*`。这证明生产注入库的同位数运行中目标路径与挂起启动资源管理真实可用；生产目标 DLL、原 Vue 注入入口、目标入口前挂起注入/StartHook 唤醒、MinHook、13 个 Winsock 钩子和抓包仍未实现，因此产品仍未完成。

最新生产目标 DLL bootstrap 切片 `run-ee7e48e5ad1143a7b2f93ceb11b7aa2f`：从当前源码全新构建，x64/Win32 各 15/15 CTest；新增两架构各 16 项真实跨进程检查，实际把生产 `wpe64-hook.dll` 注入独立目标，调用未修饰 `WpeStart` 传入 53 字节固定 bootstrap，完成三管道连接、v4 Hello/PID/位数、HookState、HookFlags 配置、未接 MinHook 时的明确 StartHook 错误、活跃会话重复入口拒绝、Detach 且宿主进程保持存活；还验证无外壳管道时目标工作线程按 100 ms 自收尾，之后可以重新启动有效会话，以及无导出 DLL/非法 session 拒绝。进程刚启动时 Toolhelp 返回 `ERROR_PARTIAL_COPY` 的真实竞态已通过有界模块快照重试修复。x64/Win32 DLL 导出均为未修饰 `WpeStart`，PE 依赖均仅 `KERNEL32.dll`。既有注入 13、目标核心 87、会话 47、管道 28、数据 346、配置 349、编辑 229（184 条原调用）、导出 41 保持；两架构仍通过 74684 原 C# 协议向量、76618 断言、600 反向封包、6416 队列向量，555 原资源/70 Vue 以及原 Vue/WebView2 首次和重启宿主均通过。manifest SHA256 `5569B6F58C8B4A7FB5EF3D5CC8BD2B0E66AA57D5CBEF2C69984A24D97A16D6A7`；完整证据目录：`E:\codex\2026-09-17\bi-a\work\bootstrap-final\run-ee7e48e5ad1143a7b2f93ceb11b7aa2f`，轻量证据归档为 `evidence/bootstrap-*`。本轮证明生产 DLL 入口和目标内真实会话，不代表 Winsock 钩子/抓包已完成；当前控制器故意拒绝 StartHook，避免把空壳冒充钩子成功。

最新 MinHook 运行时切片 `run-e735bdc7d0ad43db8ba456b585935477`：官方 MinHook 1.3.4 已按上游文件原样静态编译，依赖契约逐文件校验；新增 `HookManager` 在 x64/Win32 各通过 8 项真实 trampoline 生命周期检查。两架构从当前源码全新构建并各通过 **16/16 CTest**，包括真实注入生产 DLL 的 16 项跨进程会话检查；同时保持 74684 个原 C# 向量、76618 项协议断言、600 个反向封包、6416 个队列状态向量、原 Vue/WebView2 首次及重启验收、555 个原资源和 70 个 Vue 文件。首次全量运行在 x64 CTest 全通过后遇到原 C# oracle 进程一次无输出 `-1`，相同产物手动立即复验通过；随后从另一个全新目录完整重跑 x64/Win32 均通过，故最终证据只采用第二次完整运行。manifest SHA256 `D68542D190B99F0754FBF3E80FCEF2E2E94E0266D474C24C39D1BF94213CB44F`；完整证据目录：`E:\codex\2026-09-17\bi-a\work\minhook-final2\run-e735bdc7d0ad43db8ba456b585935477`，轻量证据归档为 `evidence/minhook-*`。此切片证明 MinHook 管理层真实运行，不代表 13 个 Winsock detour 或抓包链路已经完成；生产 `StartHook` 仍明确拒绝。
历史 WinSock 首次全新抓包切片 `run-88186cc09a034796b3836be1f59edb09`：当时 x64/Win32 各 **17/17 CTest**，并完成 50 次钩子生命周期与 20 次生产 DLL 注入/抓包/Detach 压力运行。其 manifest SHA256 `C69303A4A147CEA7DB08B6CBDB28743E09EC209F8571A2BDA6803A3228E46FB5` 只对应当时源码；随后已修复卸钩在途调用、事件有界队列、管道半帧失败、写端丢失计数、原版本地时间戳、无效目标缓冲读取和超大载荷先计数后丢弃等问题，不能再把该 manifest 冒充当前源码证据。

## 2026-09-18 源码保存检查点

用户要求停止扩展新功能，保存当前完整源码和后续开发说明。当前检查点以 `git log -1` 为准，配套文档为 `docs/后续开发说明.md`。保存前，当前源码在既有开发构建目录完成 x64/Win32 各 17/17 全量 CTest；最后一道 detour admission 生命周期修复后，又分别重编译并复验 `winsock-hook-pipeline`（x64 58 项、Win32 62 项）以及 `ipc-session-lifecycle`、`minhook-manager`、`injected-target-session`，全部通过。最终归档前会从当前提交再次记录双架构 CTest 日志和源码哈希。此检查点是可恢复开发源码，不是“全部 217 RPC 和所有执行引擎已经完成”的交付声明。

## 2026-09-18 P0 目标滤镜执行链

从检查点 `f9756c7` 接续，未重复数据、格式、IPC、MinHook 或原始抓包。新增独立 `FilterEngine`，由 HeadlessCore 把 Filters/Runtime 快照发布到生产 Winsock 控制器；实现原版普通/高级匹配、半字节通配符、排除位置、socket/端口/长度/包头门控、Priority/Sequence、Replace/Change/Intercept/显示/不显示、修改/递进/进位/随机、Filter→Filter 嵌套执行（含循环保护）、FilterLog、六项全局计数和按 GUID 迁移。相同 GUID 的执行/递进状态及全局统计在快照代际间共享原子状态，发布与 Reset 不再把并发更新覆盖回旧值。

同步 `send/sendto/recv/recvfrom`、同步 WSA 散布/聚集函数以及 Win32 `WSARecvEx` 已真实接入，发送改写、接收回写、拦截假成功、原始/修改后 PacketFrame 和 `WSAGetLastError` 保存均在生产 detour 路径完成；非空 overlapped WSA 请求不借用临时缓冲，仍列为独立未完边界。FilterLog 不再从 detour 编码或发送，而是和封包共用一次有界入环，由 writer 线程编码/投递；端口门控在 detour 只查固定大小原子缓存，`getsockname/getpeername` 只在 writer 线程调用。生产 DLL 跨进程测试现在通过真实 Filters SetConfig 把 `cross-process` 改为 `Cross-process`，并核验两端 pkt 帧。

最终 fresh 回归：`E:\codex\2026-09-17\bi-a\work\p0-filter-final3\run-c208c0efa8be48c6a41f66d4139fbb4d`，x64/Win32 各 **18/18 CTest**；滤镜纯引擎各 **42 项**，Winsock 专项 x64 **88 项**、Win32 **92 项**，生产 DLL 跨进程各 **21 项**。两架构继续通过 74684 个原 C# 向量、76618 项协议断言、600 个反向封包、6416 个队列状态向量，555 个原资源/70 个 Vue 文件保持；宿主首次与重启结果均 passed。manifest SHA256 `A99C7DA6D4E743443F6B78A54078E458EC685BD1C852980B02C85AAF9C03BE2B`，轻量证据归档为 `evidence/p0-filter-*`。

P1 已用 `bind/connect/WSAConnect/accept/WSAAccept/getsockname/getpeername/closesocket` 生命周期 hook 维护固定大小原子端点缓存：普通连接首包可执行端口门控，关闭时清除旧端口，socket handle 复用不再沿用旧值；这些 hook 只处理固定字段，不做字符串格式化、IPC 或额外同步名称查询。仍明确保留的边界是通过 `ConnectEx/AcceptEx` 扩展函数建立、且应用没有经过上述 API 的连接首包；其后 writer 或 GetSocketInfo 会补全缓存。下一切片是 P2 发送列表执行器与 Filter→Send/WareHouse；挂起启动编排、x86 helper/原 Vue 入口、自动入库、代理、映射/WPC 和剩余范围内 RPC 仍未完成。机器人运行执行器及 Filter→Robot 已明确排除。

## 2026-09-18 P1 目标内单包重放与套接字信息

`HeadlessCore` 已实现 v4 `SendPacket` / `GetSocketInfo` 的精确字段解析、空字节载荷和尾字段拒绝，以及 `Ok + bool` / `Ok + str from + str to` 精确应答。`IHookController` 的生产实现按原版 packet type 0–16 映射 WinSock 1.1/2 `send`/`sendto`：UDP 请求地址取 `to`，UDP 响应地址取 `from`；HTTP/HTTPS/WebSocket 会话编号 17–22 不作为 SOCKET 使用并返回失败。无效或已关闭 socket 不抛出目标异常，发送返回 false，地址返回两个空字符串。

主动重放保留目标进程的真实 SOCKET 所有权。若对应导出已经被 MinHook 改写，线程局部 replay 标记让 detour 在取得安全的在途 lease 后直接调用原 trampoline，避免重放再次进入抓包、滤镜或递归；未安装对应抓包 hook 时调用真实模块导出。生产跨进程测试从 pkt 帧取出目标 socket，调用 GetSocketInfo，再通过控制管道下发 `ipc-replay`，目标进程的服务端 socket 实际接收并逐字节验证该载荷。

P1 同时增加普通 Winsock 生命周期 registry，覆盖首包端口门控与 closesocket 后的句柄复用清理。目标核心新增协议测试，Winsock 专项新增 TCP/UDP 请求与响应方向、非法地址/类型、关闭 socket、TLS 旁路、首个 connected packet 端口滤镜；生产 DLL 跨进程夹具在验证完成前保持 socket 存活。机器人运行执行器及 Filter→Robot 没有加入。

最终 fresh 回归：`E:\codex\2026-09-17\bi-a\work\p1-send-final\run-8abd63fa534b40e0a377260f798531c5`，x64/Win32 各 **18/18 CTest**；目标核心各 **103 项**，Winsock 专项 x64 **113 项**、Win32 **117 项**，生产 DLL 跨进程各 **29 项**。两架构继续通过 74684 个原 C# 向量、76618 项协议断言、600 个反向封包和 6416 个队列状态向量，555 个原资源/70 个 Vue 文件保持；宿主首次与重启结果均 passed。manifest SHA256 `37BEE7659D8318E273950DAAC0E44B5547E5C9E7B00C4E407EC28458F09B76B8`，轻量证据归档为 `evidence/p1-send-*`。

## 2026-09-18 P2 发送执行与 P3 挂起唤醒增量

P2 已完成并以当前源码重新验证：`StartSend` / `StartSendList` / `StopSendList` 经过真实目标 IPC、发送快照、目标工作线程和生产 Winsock `SendPacket` 到目标进程 socket；循环/间隔、串行/并行、系统 socket、取消、成功/失败计数和 GUID 计数迁移均已接线。目标在启动、停止和完成发送时立即发送 Stats 快照，外壳将滤镜全局计数映射到 `filterExecute` / `filterPacket`，原 Vue 发送列表以 `send:running` 事件为权威状态。真实跨进程测试验证了滤镜修改、直接重放、配置发送列表重放和发送/滤镜统计回传；机器人运行执行器仍按用户要求排除。

P3-1 已完成：`TargetLink::AttachLaunched` 不再在注入后立即恢复挂起目标，而是保留 `SuspendedProcess` 到 `StartHook` 成功；`injectStartHook` 在配置和目标侧装钩成功后调用 `ResumeLaunched`，失败时保留挂起对象并由 Attach 错误路径终止回滚。AttachPid 路径保持运行中目标的无操作 Resume 兼容；TargetLink 真实回归已证明入口事件只在恢复后触发。x86 helper 的代码接线已在 P3-2 完成，剩余是原 Vue 注入页完整人工验收，以及目标提前退出/StartHook 失败/Detach 未恢复等错误矩阵。

本增量验证：x64 Release 构建成功，x64 全量 CTest **19/19** 通过；Win32 helper、Win32 hook 和 Win32 TargetLink 专项均构建通过，x64 外壳跨架构启动 Win32 目标并由 helper 完成“注入 → StartHook → Resume”回归（16 项通过）。包内普通模式窗口创建且 Responding，包内 WebView2 自测 `result=passed`、WebMessages 155、Runtime 153.0.4234.32。下一项是原 Vue 注入入口在真实目标上的人工错误矩阵，不重复修改已完成的 P2 发送链路。

## 2026-09-18 捕获过滤保存错误收尾

已修复用户界面截图中的 `saveLeachSetting` 未实现错误：方法已注册并持久化到 SystemConfig，保存前校验空条件，类别位按注入/代理分组合并，不会互相覆盖。当前配置还会随 Runtime 快照进入目标，真实 Winsock 抓包在写入列表前执行捕获过滤。若旧包仍在运行，需先关闭旧窗口再启动新包以加载更新后的 wpe64-app.exe。

## 2026-09-18 P3-1 真实挂起目标注入回归

P3-1 已完成：新增真实 `TargetLink::AttachLaunched` 回归目标和事件探针，验证 `CREATE_SUSPENDED` 目标在 IPC 建立、配置同步、`StartHook` 成功前不会进入用户入口；仅 `ResumeLaunched` 成功后入口事件才触发。挂起期间的加载器模块快照竞态已在 `src/shell/process_injector.cpp` 做受限回退，运行中注入路径不受影响。

验证结果：当前 Release 全量 CTest **19/19 passed**，TargetLink 专项 `target-link-suspended-launch` 通过。下一步为 P3-2：x86 辅助注入器和原 Vue 注入页人工验收；目标提前退出、StartHook 失败、Detach 时仍挂起等错误矩阵仍需补齐。机器人运行执行器继续不实现。

## 2026-09-18 P3-2 x86 辅助注入器与注入入口接线

P3-2 的实现已完成：新增 `wpe64-x86-helper.exe`，由 x64 外壳在目标为 32 位且宿主为 64 位时通过受控命令行和命名事件完成跨位数 `InjectAndStart`。启动模式保持 `CREATE_SUSPENDED`，helper 在注入并执行 `StartHook` 成功后只发出 ready，外壳收到 `injectStartHook` 后再发 resume；Attach 失败、外壳退出、Detach 或窗口关闭都会发 abort 并清理 helper/挂起目标。32 位外壳仍直接走本地注入器，避免重复启动 helper。

原 Vue `InjectView` 的进程/窗口/文件/快速注入入口没有另起一套逻辑，继续复用 `TargetLink`；`main.cpp` 现在从包目录自动发现 `wpe64-hook-x86.dll` 和 `wpe64-x86-helper.exe`，缺失时回到原有错误提示。`build-test.ps1` 已加入 Win32 helper 构建、跨架构 TargetLink 回归和包文件清单。

验证结果：x64 Release 全量 CTest **19/19 passed**；Win32 helper、hook、TargetLink 专项构建成功，Win32 `target-link-suspended-launch` **1/1 passed**；x64 外壳真实启动 Win32 目标并经 helper 完成“注入 → StartHook → Resume”，TargetLink **16/16 passed**。并行 Win32 编译曾触发 MSBuild 页面文件不足，构建脚本已改为串行，随后 Win32 全量 CTest **19/19 passed**。仍待人工验证的是原 Vue 注入页连接真实 32 位目标时的权限、窗口选择和提示文案；自动错误矩阵已在 P3-3 补齐。机器人运行执行器及 Filter→Robot 继续不实现。

## 2026-09-18 P3-3 注入错误矩阵与失败清理

P3-3 已完成。`TargetLink` 现在在 x86 helper ready 等待期间同时观察 ready 事件和 helper 进程句柄，能区分超时与 helper 提前退出；Attach 模式的 helper 在 `InjectAndStart` 成功后显式发 ready。外壳 `injectAttach`、`injectQuick`、`injectStartHook` 的配置、StartHook、Resume 失败路径都会调用 `Detach` 清理，避免留下挂起目标或等待中的 helper。

新增 `target-link-error-matrix`：x64 本地测试 **33 项**覆盖缺失 DLL、StartHook 错误回传、StartHook 前 Detach、helper ready 超时和 x86 运行中 Attach；双架构构建脚本另执行 x64 → Win32 的相同矩阵。错误夹具只验证生命周期和清理，不把机器人运行执行器加入生产路径。

## 2026-09-18 P4-1 滤镜命中后的自动入库执行链

P4-1 已完成。真实生产目标命中 WareHouse 执行滤镜时，FilterEngine 将改写后的 bytes 生成 `StoreAdded`，目标 writer 经事件管道交给外壳，外壳以有界 `DataWorker::SubmitStoreEvent` 串行提交，`DataService::ApplyStoreEvent` 事务写入 `WareHouseData`、执行仓库容量裁剪并发布 `feed:replace(list=11)`。目标 detour 线程不执行数据库、文件或 UI 操作。

新增的 `injected-target-session` 回归会创建仓库、配置真实 WareHouse filter、驱动独立目标 TCP 流，验证 StoreAdded GUID/改写后字节、worker 提交结果、仓库 feed 计数和 SQLite worker 重启持久化；现有 `data_service_test` / `data_worker_test` 继续覆盖坏帧、陈旧仓库、限额、队列与提交异常。x64 回归 **46 项通过**，Win32 同一回归通过，机器人运行执行器仍未加入。

## 2026-09-18 P4-2 滤镜命中后的 Send 执行链

P4-2 已完成。真实目标命中 `execute_type=Send` 的滤镜后，目标 writer 线程把 trigger 交给 `HeadlessCore::StartSendById`，发送 worker 使用快照中的真实 socket、端点和 bytes 调用生产 `SendPacket`；目标端通过 Stats 回传该 Send GUID 的成功计数。新增跨进程阶段验证了第二个匹配 TCP 包、滤镜修改结果、trigger replay、Stats 成功回传，并与 P4-1 仓库入库、直接 SendPacket、发送列表执行放在同一条生产 DLL 回归中。

x64/Win32 专用 `injected-target-session` 均通过，当前回归 **51 项**。机器人运行执行器仍未加入；下一步可选择代理、映射或 WPC 的真实执行链。

## 2026-09-18 P4-3 代理执行链（已完成）

- 选定 SOCKS5 TCP CONNECT 作为本轮真实链路；`Socks5Runtime` 绑定配置的 IPv4/IPv6 地址和端口，独立 accept/client 线程完成握手、可选 RFC1929 用户名/密码认证、IPv4/域名/IPv6 目标解析、5 秒连接超时和双向 relay。
- `Host::startProxy` 从 native-only `__proxyRuntimeConfiguration` 快照装载代理设置与启用账号，`stopProxy` 确认收尾；`getStats` 返回真实 `proxyRunning`、活动 TCP、请求/响应、上下行字节和错误计数，原 Vue 继续使用既有 RPC/状态界面。
- `Only_WPC_Client`、UDP、映射和 WPC 不被静默降级或绕过：WPC-only 启动明确返回未接入错误；HTTP/HTTPS 仅提供正向代理与 CONNECT 隧道，不宣称已实现映射或 WPC；机器人执行器不加入。
- 保护点：账号解密不在 `DataService::Methods()` 中暴露；目标连接非阻塞并有界等待；停止先关闭监听器再等待所有 worker；所有计数使用原子变量。
- 验证：x64/Win32 Release 的 `socks5-runtime` 与 `http-proxy-runtime` 专项均 **2/2 passed**。新增回归使用真实本地 TCP server，覆盖 SOCKS5 no-auth/用户名密码认证、HTTP absolute-form、CONNECT relay、Basic 认证、重启、停止与计数。


## 2026-09-18 P4-4 HTTP/HTTPS 代理真实执行链（已完成）

- 新增 `HttpProxyRuntime`，与 `Socks5Runtime` 共用 P4-3 的 native Host 生命周期：监听、可选认证、非阻塞目标连接、双向 relay、原子统计、停止时关闭 listener 并等待 worker。
- 普通 HTTP 请求接受 absolute-form（例如 `GET http://host/path`），转发前重写为 origin-form 并剥离 `Proxy-Connection`、`Proxy-Authorization`；CONNECT 请求返回 `200 Connection Established` 后进入透明 HTTPS 隧道。
- `startProxy` 同时读取 `enableHttp/httpPort` 与 SOCKS5 配置；端口冲突、认证账号缺失、任一监听失败都会返回错误并回滚已启动的监听。`getStats` 汇总两条链的真实请求、响应、活动连接、上下行字节和错误。
- 验证：x64/Win32 Release 的 `socks5-runtime` 与 `http-proxy-runtime` 专项均通过；`socks5-runtime` 新增真实 UDP ASSOCIATE echo 回归；机器人运行执行器和 WPC 仍不在范围内。

## 2026-09-18 P5-1 HTTP 映射执行链（已完成）

- `ProxyMapLocal` 和 `ProxyMapRemote` 的启用开关、顺序和规则字段现在随 native-only 运行快照进入 `HttpProxyRuntime`；浏览器 RPC 仍只负责原有 CRUD/启用/排序，不暴露代理账号或额外敏感数据。
- 普通 HTTP 请求按大小写不敏感的源 host、端口和路径边界匹配。命中本地映射时直接返回本地文件（64 MiB 上限、读取失败返回明确 HTTP 错误）；命中远程映射时重写目标 host/port/path，再走原有 5 秒连接超时、代理头剥离、origin-form 转发和 relay。两类同时启用时本地规则优先，各自保持列表顺序。
- CONNECT 只建立透明 HTTPS 隧道，不尝试匹配加密后的路径；非 HTTP 映射协议返回 501，不静默降级。`getStats` 新增 `mappingHits`、`mappingMisses`、`mappingErrors`，并在停止/回滚时随 HTTP 运行时清零。
- HTTP 专项回归新增本地文件响应、远程路径前缀重写、未命中回退和映射计数；x64/Win32 Release 均通过。机器人运行执行器继续不加入，WPC 仍是后续切片。

## 2026-09-19 P5-2 SOCKS5 UDP 代理执行链（已完成）

- `Socks5Runtime` 现在支持 RFC 1928 `UDP ASSOCIATE`。认证后的控制连接创建同地址 UDP relay，返回实际绑定端口；客户端 UDP 包支持 IPv4、IPv6 和域名目标，转发到真实 UDP socket，目标响应重新封装源地址后返回。
- UDP 关联与控制 TCP 绑定生命周期。控制连接关闭、代理停止或异常时会回收 UDP relay；`getStats` 新增真实 `udpReq`、`udpResp`、`udpConn`，并把 UDP 字节计入上下行统计，`tcpConn` 不重复计算 UDP 关联。
- x64/Win32 `socks5-runtime` 均通过真实本地 UDP echo 回归，覆盖 ASSOCIATE、包头解析、ping/pong、统计、重启、停止和原有认证链兼容。WPC、外部代理、防火墙实际判定和机器人运行执行器继续不加入。

## 2026-09-19 P5-3 WPC HTTP 发布服务（已完成当前可证实范围）

- 新增 `WpcRuntime`，由 `getRemoteSetting` / `saveRemoteSetting` 读取和持久化 `SystemConfig` 远程设置，真实绑定本机 IPv4/IPv6 地址；启用时启动 listener，禁用或退出时关闭 listener 并等待全部 client worker。
- `/ProxyCap/GetServerList` 发布启用节点、24 类规则和节点 URL；`/ProxyCap/GetNoticeList` 发布启用公告字段；两条客户端拉取接口不要求管理认证。`/healthz` 采用 HTTP Basic，回传运行状态、活动连接、请求/响应和错误计数。服务端/规则/公告保存后通过快照更新让下一次请求立即看到新内容。
- `getStats` 增加 `wpcRunning`、`wpcConn`、`wpcReq`、`wpcResp`、`wpcErrors`。x64/Win32 Release 新增 `wpc-runtime` 回归均通过，完整 CTest 从 22 项增至 23 项并全部通过。
- 边界：没有把未知私有 SOCKS WPC 握手、设备注册/令牌、断线重连或 `Only_WPC_Client` 代理数据流量冒充成已完成；这些仍需真实协议证据和后续专项测试。
