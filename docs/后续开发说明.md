# WPE64 C++ 源码检查点与后续开发说明

更新日期：2026-09-18
工程目录：`E:\codex\2026-09-17\bi-a\outputs\WPE64-Cpp`

## 1. 文档用途

本文用于保存当前可恢复开发状态，并给后续开发者一条不重复返工的接续路径。它只描述源码、功能和验证事实，不对项目用途或授权状态作任何推断。

当前工程是 **C++20 原生重写检查点**，不是全部功能已经交付的最终成品。原 Vue3 页面和资源仍保留；C# 原工程只作为格式、协议和行为对照，不是生产运行依赖。

## 2. 规范与只读输入

继续开发前必须依次读取：

1. `CONTRIBUTING.md`
2. `contracts/active-spec.json`
3. `docs/用户提供-重写工程文档-更新版.md`
4. `docs/无人值守接续.md`
5. 本文

只读输入：

- 原源码 ZIP：`C:\Users\Administrator\Downloads\WinsockPacketEditor-master.zip`
- ZIP SHA256：`1F524B84621F064ACD91C3F9D764D1ADDF19AC42019E7FCEDE11BF55EA536113`
- 解压对照源码：`E:\codex\2026-09-17\bi-a\work\source-audit\WinsockPacketEditor-master\WinsockPacketEditor`
- 更新版工程文档哈希见 `contracts/active-spec.json`
- 原 Vue、真实业务输入和 `runtime/2.3.0/WPE.db` 不得被测试覆盖或清空；测试必须使用隔离目录。

## 3. 当前已经实现并真实接线的范围

### 3.1 原界面与宿主

- 原 Vue3、70 个 Vue 文件和全部静态资源保留。
- 原生 C++ WebView2 宿主、五消息前端桥、文件选择授权、密码框、剪贴板和首次/重启页面测试已有实现。
- WebView2 Runtime 是外部运行环境，不应打包成业务源码或用假页面替代。

### 3.2 数据与原格式

- SQLite 20/20 表。
- 四类父列表、滤镜/发送/机器人/仓库、白名单、黑名单、代理账号、自动入库、双向映射、WPC 服务端/规则/通知等数据接线。
- `.fp/.sp/.rp/.whp/.sc/.whs/.wl/.bl/.pa/.pas/.pml/.pmr/.sb` 的明文及原 AES 兼容路径。
- `.sb` 14/14 分组，原 XML 字段顺序、空/null、BOM、换行、密码派生和 AES-CBC 行为已有黄金测试。
- 当前注册约为 126 个数据接口 + 10 个宿主接口；原清单共 217 个接口，仍有 81 个未注册。注册数量不是功能完成率。

### 3.3 IPC、目标核心和注入基础

- ctl/pkt/evt 三条真实 Windows 命名管道、当前用户 ACL、overlapped I/O、取消和帧上限。
- Shell/Target v4 会话、Hello、Ping、watchdog、Detach、错误应答、pkt/evt 接收线程。
- 事件通道为原版口径的有界队列：8192 条 / 16 MiB，丢最旧、累计并生成日志，唯一后台线程写 evt；事件生产者不直接阻塞写管道。
- 目标无头核心支持 HookFlags、Filters、Runtime、Sends、Robots 五类快照，统计、ResetStats、HookState/Fatal/Stats 事件。
- x64/Win32 同位数 `LoadLibraryW + WpeStart` 原生注入基础和生产 `wpe64-hook.dll`；`DllMain` 不做业务初始化。

### 3.4 MinHook 与真实原始抓包

- 官方 MinHook 1.3.4 已静态纳入。
- 生产 `HookManager` 使用进程全局引用计数，只操作自己注册的目标；停用、移除或反初始化失败时保留状态供重试。
- 已接 13 个原版 Winsock 签名规则：
  - wsock32：`send/sendto/recv/recvfrom`
  - ws2_32：`send/sendto/recv/recvfrom/WSASend/WSASendTo/WSARecv/WSARecvFrom`
  - Win32：`WSARecvEx`
  - x64 按原版规则跳过 `WSARecvEx`
- detour 只做安全读取、不可变字节复制、原子计数和入环；地址解析、PacketFrame 编码、pkt 管道写入在专用线程完成。
- 64 MiB / 65536 条封包丢旧环、每批最多 256 条 / 1 MiB、极速模式、11 项实时计数和累计 Dropped 事件已接。
- 已处理无效指针、超大载荷、写端失败、端点解析失败、外壳断开和半帧超时；这些失败计入丢弃而不伪装成成功抓包。
- 卸钩时先在共享 admission gate 下清除 active owner，再停用 hook，等待已进入 detour 的调用退出；若阻塞 `recv` 超时则保留 owner/trampoline，并由安全退休线程重试，避免释放后跳转。

### 3.5 目标侧滤镜执行链

- `FilterEngine` 从 Filters/Runtime 快照编译运行表，经原子裸指针发布；封包热路径只使用原子读者计数和指针读取，不获取发布互斥锁，旧表在发布线程观察到静默点后回收。相同 GUID 的执行/递进状态及全局计数跨表共享原子运行态，避免发布或 Reset 与并发封包互相覆盖。
- 已实现启用状态、12 类函数、socket、端口、长度、包头门控，普通/高级匹配、半字节通配符、排除位置、绝对/相对位置、Priority/Sequence 顺序。
- 已实现 Replace、Change、Intercept、NoModify_Display、NoModify_NoDisplay，以及修改、递进、进位、连续递进和随机位置。
- 已实现 Filter→Filter 联动执行及固定深度循环保护；Send、WareHouse 两种联动仍待实现，且不会被伪计为执行成功。Robot 联动及机器人运行执行器已按用户要求移出交付范围。
- 同步 `send/sendto/recv/recvfrom`、同步 `WSASend/WSASendTo/WSARecv/WSARecvFrom` 与 Win32 `WSARecvEx` 已接真实改写/拦截；原始和修改后字节及动作进入 PacketFrame，NoModify_NoDisplay 不进入 pkt 队列。
- 滤镜执行次数、六项全局计数、ResetStats 和 FilterLog 事件已接；FilterLog 随封包工作项一次入环，由 writer 线程编码并发送，detour 不再同步构造 IPC 或调用事件发送器。极速模式仍执行滤镜和计数，但不产生 FilterLog/pkt 帧。
- 端口门控在封包 detour 只查固定大小原子端点缓存；`bind/connect/WSAConnect/accept/WSAAccept/getsockname/getpeername/closesocket` 生命周期 hook 只合并或清除端口元数据，不做字符串格式化、IPC 或额外同步查询。普通 `connect/accept` 路径的首个 connected 封包和 socket handle 复用已覆盖；通过 `ConnectEx/AcceptEx` 扩展函数、且应用未经过上述生命周期 API 的首包仍须单独补扩展函数注册。
- 非空 overlapped WSA 请求仍保持原缓冲和原异步生命周期，不使用临时改写缓冲；其完成期滤镜需要独立的 overlapped 上下文设计，不能把同步实现冒充为已完成。

## 4. 明确尚未完成的功能

以下项目不可在报告中写成“已完成”：

1. **发送执行器**：发送列表、循环/间隔、停止和发送/状态回传已完成；滤镜的 Send/WareHouse 联动已在 P4-1/P4-2 通过真实目标回归。机器人运行执行器及 Robot 联动按用户要求不实现；机器人数据、文件格式和编辑兼容保留。
2. **异步 WSA 完成期滤镜**：非空 overlapped 请求目前保留原异步调用；尚未建立完成例程/事件/IOCP 统一生命周期和安全的改写缓冲所有权。
4. **挂起启动完整编排**：P3-1 已验证“目标入口执行前完成注入，StartHook 成功后才恢复”；P3-2/P3-3 已覆盖 x64→Win32 helper 顺序、提前退出、StartHook 失败、Detach 和 helper 超时的自动化生命周期回归。真实用户目标上的权限和提示文案仍需人工验收。
5. **x86 辅助注入器和原 Vue 注入入口**：x64 外壳现在可调度 Win32 helper 完成运行中与挂起启动注入；原 Vue 进程/窗口/文件/快速注入入口已接入，仍需在真实目标窗口上完成人工验收。
6. **自动入库执行链**：P4-1 已接通滤镜命中 → StoreAdded → DataWorker → SQLite 仓库；P4-2 又接通滤镜命中 → Send → 目标 socket；P4-3 已接通 SOCKS5 TCP CONNECT 代理，HTTP/UDP、映射/WPC 运行引擎仍待后续切片。
7. **代理运行引擎**：SOCKS5 TCP CONNECT、认证、连接、统计已接；HTTP/UDP、外部代理、进程转发、防火墙实际判定尚未接。
8. **映射与 WPC 运行引擎**：HTTP 映射转发、远程 WPC 连接、规则下发和通知传输尚未接。
9. **剩余 RPC 与最终 UI 验收**：`contracts/rpc-status.json` 和 `docs/全部217个接口接线清单.csv` 需逐项更新；全部页面入口、主题/语言、TopMost、文件按钮组合仍需最终核验。
10. **最终发布验收**：尚未完成从干净源码的最终双架构构建、完整行为矩阵、最终可运行包和最终交付 ZIP。

## 5. 推荐的后续开发顺序

### P0：滤镜执行链（同步路径已完成）

对照原源码：

- `ClassObject/WinSockHook.cs`
- `Operate.cs` 中 `FilterConfig.List.DoFilterList`
- `ClassObject/Core/Ipc/WpeCore.cs`

已完成范围：

1. Filters/Runtime 快照已发布到生产控制器，纯引擎固定预期覆盖门控、匹配、动作、递进、顺序和统计。
2. 真实 TCP 与散布/聚集 WSA 同步路径已验证发送改写、接收改写、拦截、PacketFrame 原始/修改字节和跨进程生产 DLL。
3. overlapped 完成期语义明确保留为后续独立切片，没有借同步测试作完成声明。

### P1：SendPacket、GetSocketInfo 与 socket registry（已完成）

1. 已按 v4 协议实现 `i32 socket + i32 type + str from + str to + bytes`、`i32 socket` 的精确解析和尾字段拒绝；空指针字节载荷明确拒绝。
2. packet type 0–16 按原版选择 WinSock 1.1/2 的 `send` 或 `sendto`，UDP 请求取 `to`、UDP 响应取 `from`；HTTP/HTTPS/WebSocket 会话编号 17–22 不冒充 SOCKET，返回 `false`。
3. 主动重放使用 TLS 标记；若目标导出已被 MinHook 改写，detour 直接走原 trampoline，不再次抓包或执行滤镜。无 Hook 时使用真实导出，仍在持有该句柄的目标进程执行。
4. `GetSocketInfo` 在目标控制线程查询本机/远端 IPv4 地址并返回原协议的两个字符串；无效/已关闭 socket 返回空字符串。
5. 生命周期 registry 使用固定大小原子缓存，覆盖普通 bind/connect/accept/name/close 路径的首包端口和句柄复用清理；`ConnectEx/AcceptEx` 首包边界仍明确保留。
6. 单元、真实 TCP/UDP 和生产 DLL 跨进程测试覆盖精确应答、非法尾字段/地址/类型、目标句柄地址查询、TLS 重入旁路、首包端口滤镜，以及目标进程实际收到重放字节。

### P2：发送列表执行器（不含机器人运行引擎，已完成）

1. 已实现 StartSend / StartSendList / StopSendList，不实现机器人指令运行状态机。
2. 所有执行线程具有明确 owner、取消令牌、join/退休顺序；不依赖进程退出清理。
3. 计数由执行者维护，通过 Stats 镜像；配置替换按 GUID 迁移运行计数。

机器人列表、导入导出、数据库和指令编辑继续保留以兼容既有数据与原界面；`startRobotList`、`stopRobotList`、`startRobotEdit`、`stopRobotEdit`、`getRobotEditProgress` 以及 Filter→Robot 不纳入最终运行验收，也不以空成功处理器冒充实现。

### P3：注入入口和挂起启动（代码已完成，人工矩阵待验收）

1. 已做独立 x86 helper，并通过命令行和命名事件完成无裸指针的跨位数调度。
2. 已实现入口前挂起：创建目标 → helper 注入 → 三管道 Hello → SetConfig → StartHook → 明确成功 → 恢复主线程。
3. 已用 TargetLink 回归证明目标入口前没有执行用户代码；首个网络包捕获由既有生产 DLL/滤镜回归覆盖。
4. 原 Vue 的进程/窗口/文件/快速注入入口已复用同一 TargetLink，不修改页面视觉和交互契约；剩余为真实用户目标人工错误矩阵。

### P4：自动入库、代理、映射和 WPC

按“已有配置数据 → 单一真实执行路径 → 事件/统计 → UI 验收”逐个推进，禁止同时铺多个空壳。

### P5：剩余 RPC、完整验收和最终打包

1. 逐项更新 217 接口清单，状态只允许“未注册 / 已注册未运行 / 已运行未验收 / 已验收”。
2. 双架构、原 Vue、原格式、异常和生命周期全部从干净源码重跑。
3. 最终包必须实际启动，核验 PE 位数、导出、依赖、资源哈希、数据库迁移和卸载清理。

## 6. 关键源码位置

- `src/common/ipc_codec.*`：原始字段编解码
- `src/common/ipc_pipe.*`：Windows 命名管道
- `src/common/ipc_session.*`：Shell/Target 会话与事件有界队列
- `src/common/packet_frame.*`：封包帧
- `src/common/packet_ring.*`：封包有界环
- `src/target/headless_core.*`：目标控制协议和配置快照
- `src/target/filter_engine.*`：滤镜编译、匹配、动作、计数与无锁发布
- `src/target/hook_manager.*`：MinHook owner/runtime 生命周期
- `src/target/winsock_hook.*`：13 签名、detour、计数、抓包写线程
- `src/target/injected_runtime.*`、`src/target/dllmain.cpp`：生产目标 DLL 入口
- `src/shell/process_injector.*`：同位数注入基础
- `src/shell/data_*`、`editor_*`、`config_xml.*`：数据与原文件格式
- `tests/winsock_hook_test.cpp`：真实 TCP/UDP、无效指针、超大载荷、阻塞 recv 退休和丢弃测试
- `tests/filter_engine_test.cpp`：滤镜语义、边界和并发快照发布固定预期
- `tests/injected_session_test.cpp`：真实跨进程生产 DLL 抓包测试
- `tests/ipc_session_test.cpp`：真实三管道、心跳、Detach 和 evt 有界队列测试

## 7. 构建与验证

推荐使用项目脚本创建全新外部构建目录：

```powershell
Set-Location 'E:\codex\2026-09-17\bi-a\outputs\WPE64-Cpp'
.\build-test.ps1 -BuildRoot 'E:\codex\2026-09-17\bi-a\work\checkpoint-verify' -Architecture x64,Win32
```

快速开发构建：

```powershell
cmake -S . -B 'E:\codex\2026-09-17\bi-a\work\dev-x64' -A x64
cmake --build 'E:\codex\2026-09-17\bi-a\work\dev-x64' --config Release --parallel
ctest --test-dir 'E:\codex\2026-09-17\bi-a\work\dev-x64' -C Release --output-on-failure
```

Win32 将 `-A x64` 改为 `-A Win32`，并使用另一个构建目录。编译警告按错误处理；不得在源码树内生成构建产物。

## 8. 当前验证边界

P0 滤镜切片当前已验证：

- 从当前源码的全新外部目录构建，x64、Win32 各通过 18/18 CTest。
- 滤镜纯引擎两架构各通过 42 项；Winsock 专项 x64 88 项、Win32 92 项，覆盖发送/接收改写、双向拦截、connected 端口缓存、writer 延迟 FilterLog、NoModify_NoDisplay 仅日志工作项，Win32 另覆盖 `WSARecvEx`。
- 生产 DLL 跨进程会话两架构各通过 21 项，包含真实 Filters 快照、发送改写和原始/修改后 pkt 帧。
- 两架构继续通过 74684 个原 C# 向量、76618 项协议断言、600 个反向封包、6416 个队列状态向量；555 个原资源和 70 个 Vue 文件保持。
- 最终证据目录为 `E:\codex\2026-09-17\bi-a\work\p0-filter-final3\run-c208c0efa8be48c6a41f66d4139fbb4d`，manifest SHA256 `A99C7DA6D4E743443F6B78A54078E458EC685BD1C852980B02C85AAF9C03BE2B`；轻量日志归档为 `evidence/p0-filter-*`。

这些结果只证明当前已实现切片，没有证明第 4 节的未完成功能。

## 9. 恢复开发步骤

1. 解压源码检查点或进入工程目录。
2. 运行 `git status --short`，确认没有未知改动。
3. 运行 `git log -1 --oneline`，把该提交作为恢复基线。
4. 重新读取第 2 节五个文档。
5. 在新的外部 build root 跑 x64/Win32 基线测试。
6. P1 `SendPacket` / `GetSocketInfo`、P2 发送列表、P3 挂起顺序/x86 helper/错误矩阵、P4-1 滤镜命中自动入库、P4-2 Filter→Send 以及 P4-3 SOCKS5 TCP CONNECT 已完成；后续只推进 HTTP/UDP、映射或 WPC 的真实执行链，不实现机器人运行执行器，也不要重复重写已经完成的数据表、AES/XML、IPC、MinHook、原始抓包或既有 P0/P1/P2/P3/P4 链路。
7. 每个切片都要更新 `docs/无人值守接续.md`、RPC 状态、测试和证据；不得把历史 manifest 当成当前源码证据。

## 10. 不可破坏的实现约束

- 生产代码保持 C++20；原 C# 只作对照。
- 保留全部原 Vue 和资源，不用新界面掩盖未实现功能。
- detour 不做文件、数据库、同步管道写、UI 或字符串格式化。
- 跨线程数据必须有明确所有权和不可变生命周期。
- 所有队列有界、满时丢旧并计数；不能用阻塞目标网络线程换取“零丢包”。
- 帧写超时后不得继续复用可能已有半帧的流。
- 卸钩不能在仍有已进入 detour 的调用时释放 trampoline 或 owner。
- x64/Win32 必须跑同一套适用测试，架构差异必须明确记录。
- 任何“完成”声明必须有当前源码、当前构建和真实运行证据。

## 当前检查点：P3 挂起注入编排（2026-09-18）

P2 发送执行器与状态回传已完成：真实目标 IPC →发送快照→目标工作线程→生产 Winsock SendPacket→目标 socket；Start/Stop/完成均发送即时 Stats，前端以 send:running 为权威状态。真实跨进程测试已覆盖滤镜修改、直接重放、发送列表重放、滤镜计数和发送计数。

P3-1 已落地：AttachLaunched 保留挂起进程句柄，不在注入后立即 Resume；injectStartHook 只有在同步配置和目标侧 StartHook 成功后才调用 ResumeLaunched。AttachPid 对运行中目标保持兼容。失败路径会停止会话并释放未恢复的 SuspendedProcess，由其析构终止挂起子进程；新增 TargetLink 真实挂起入口回归证明入口只在恢复后触发。P3-2 已落地 x64→Win32 helper、包目录资源发现和原 Vue 入口接线；P3-3 已落地缺失 DLL、StartHook 失败、Detach-before-resume、helper 超时和 x86 运行中 Attach 的自动化错误矩阵。下一步为真实用户目标上的权限、窗口选择和提示文案人工验收。

## 2026-09-18 捕获过滤设置修复与目标链路接入

当前增量修复了原 Vue「过滤设置 / Capture Filter」保存时报 `尚未实现的方法: saveLeachSetting` 的根因。`getLeachSetting` / `saveLeachSetting` 已注册到 C++ DataService，读取和保存 SystemConfig 的六项条件、过滤方式、十二位类别掩码；勾选条件为空会返回与原版一致的错误，代理/注入弹窗只提交当前类别组时，另一组位不会被清空。

同一快照已从外壳通过 Runtime IPC 下发到目标进程。Winsock 捕获路径在进入列表前执行 socket、IP、端口、包头、十六进制内容、长度和包类型匹配，支持「只显示命中 / 不显示命中」两种语义；非法条件按原版失败开放，不影响目标线程的网络调用。新增 DataService、HeadlessCore 和真实 Winsock pipeline 回归覆盖保存/重启、模式分组保留、跨进程配置以及命中包抑制。

## 2026-09-18 P3-1 真实挂起目标注入回归

P3-1 已完成。新增 `wpe64-target-link-suspended-launch` 跨进程回归：真实 `TargetLink::AttachLaunched` 创建 `CREATE_SUSPENDED` 目标、注入生产 `wpe64-hook.dll`、建立三管道并完成 Hello；目标入口事件在 Attach 和 `StartHook` 完成后仍未触发，只有 `ResumeLaunched` 成功后才触发，证明恢复顺序为“注入 → IPC/配置 → StartHook → 恢复主线程”。

同时修复挂起目标的注入竞态：初始线程仍处于加载器初始化阶段时，Toolhelp 模块快照可能持续返回 `ERROR_PARTIAL_COPY`，`process_injector` 对 `suspended_launch` 仅对 `kernel32.dll` 使用受限的同位数系统 DLL 基址回退，运行中目标仍坚持模块表路径；DLL 加载完成后继续用目标模块表确认。错误路径仍回收远程内存、管道会话和未恢复子进程。

本轮构建与验证：Release 全量 CTest **19/19** 通过（新增 `target-link-suspended-launch`），其中 TargetLink 专项通过；P2 发送、滤镜、生产 DLL 跨进程回归保持通过。x86 helper 和原 Vue 注入页人工验收仍属于后续 P3-2，不加入机器人运行部分。

## 2026-09-18 P3-2 x86 helper 与注入入口接线

P3-2 的实现部分已完成：新增只允许 Win32 构建的 `wpe64-x86-helper.exe`。x64 `TargetLink` 会先判断目标 PE/进程位数；x64 目标继续走本地注入，x86 目标改由 helper 负责 `LoadLibraryW + WpeStart`。挂起启动时 helper 持有目标进程和主线程句柄，建立 IPC 后等待命名事件；外壳收到 `StartHook` 成功再发 Resume，失败/Detach 则发 Abort 并由 helper 析构终止未恢复目标。Win32 外壳本身是同位数注入器，不会错误地再启动 helper。

主程序 `TargetLink` 构造已接入 `wpe64-hook-x86.dll` 和 helper 路径选择；现有原 Vue `InjectView` 的进程、窗体、文件、快速注入调用链保持不改视觉契约，成功/失败/断开状态继续通过 `inject:state` 和原 Toast 回传。`build-test.ps1` 已把 Win32 helper 构建和 x64→Win32 TargetLink 回归纳入双架构证据。

当前人工验收边界：尚未在用户实际目标窗口上点击验证权限、目标提前退出和 `StartHook` 失败提示；这些属于真实 UI 验收，不用固定测试靶子冒充最终用户目标。机器人运行执行器继续不实现。

## 2026-09-18 P3-3 注入错误矩阵与失败清理

P3-3 已完成可重复的 TargetLink 错误矩阵：缺失目标 DLL 在挂起子进程逃逸前失败；目标侧 `StartHook` 返回错误时，外壳把错误传回并由 UI 注入失败路径调用 `Detach`，释放 IPC、helper 和未恢复的挂起目标；`Detach` 发生在 `StartHook` 前也会终止挂起子进程；x64 外壳的 x86 helper ready 等待有明确超时、helper 提前退出检测和有界清理；x86 helper 的运行中 Attach 模式在注入完成后显式发出 ready，不再卡在外壳等待阶段。

新增 `target-link-error-matrix` 测试夹具和 `wpe64-target-link-error-hook.dll` / `wpe64-target-link-helper-stub.exe`，覆盖缺失 DLL、StartHook 失败、Detach-before-resume、helper 超时以及 x86 运行中 helper Attach。双架构构建脚本还会执行 x64 外壳 → Win32 helper 的跨架构错误矩阵。真实用户目标上的权限、窗口选择和提示文案仍需人工点击验收。

## 2026-09-18 P4-1 滤镜命中后的自动入库执行链

P4-1 已完成真实链路接线与回归验证。命中的滤镜若配置 `execute=true`、`execute_type=WareHouse`，目标端 `FilterEngine` 会把滤镜处理后的字节封装为 `IpcEvent::StoreAdded`；Winsock writer 线程通过事件管道投递到外壳，`Host::HandleTargetFrame` 交给有界 `DataWorker::SubmitStoreEvent`，再由 `DataService::ApplyStoreEvent` 在单 worker 事务中写入 `WareHouseData`、按 `StoresLimit` 裁剪并发布仓库 `feed:replace(list=11)`。事件队列满、事件过大、仓库已删除或界面 feed 投递失败均保持明确的丢弃/提交结果，不在目标 detour 线程执行 SQLite 或 UI 操作。

`tests/injected_session_test.cpp` 现在先创建真实仓库，再把生产滤镜快照下发到独立目标进程，发送匹配 TCP 数据，验证：生产 DLL 发出指定仓库的 `StoreAdded`、数据 worker 提交成功、仓库 feed 的 `DataCount` 更新、落库字节为改写后的 `Cross-process`，以及 worker 重启后仍能读取同一行。该回归与既有 `data_service_test` / `data_worker_test` 的限额、陈旧仓库、坏帧和事务异常覆盖互补；机器人运行部分仍未加入。

当前验证：x64 `injected-target-session` **46 项通过**；Win32 同一回归通过；`native-data`、`data-worker` 均通过。一次并行 Winsock 专项出现既有时序抖动后，单独复跑通过，未观察到 P4-1 失败。下一项应进入 P4-2（代理/映射/WPC 中选择一个真实执行链），不要把自动入库规则页面重复实现为另一套运行时。

## 2026-09-18 P4-2 滤镜命中后的 Send 执行链

P4-2 已完成真实目标回归。目标端滤镜命中后由 `FilterEngine` 生成 `FilterExecuteType::Send` trigger；Winsock writer 线程把 trigger 交给 `HeadlessCore::StartSendById`，目标发送 worker 使用当前 Send 快照和真实 socket 调用生产 `SendPacket`，并通过 Stats 回传成功计数。整个过程不需要外壳轮询，也不把发送操作放回 detour 线程。

`tests/injected_session_target.cpp` 新增第二个匹配 TCP 阶段：测试先完成 P4-1 的 WareHouse 入库，然后由外壳下发带真实 socket/端点的 Send 快照，将同一个滤镜快照切换到 `execute_type=Send`，释放第二个匹配包，目标端实际收到“滤镜修改后的原包”和“Send trigger replay”两段数据。`tests/injected_session_test.cpp` 同时核验目标收到重放、Filter→Send 的 Stats 成功计数、既有直接 `SendPacket`、发送列表执行和仓库持久化，专用跨进程回归达到 **51 项**。

本轮没有加入机器人运行执行器；独立 AutoStores 包头规则、代理、映射和 WPC 运行引擎仍是后续范围。

## 2026-09-18 P4-3 SOCKS5 代理真实执行链

P4-3 选择代理链落地：新增 `src/shell/socks5_runtime.*`，由原 Vue 的 `startProxy` / `stopProxy` 直接控制原生 SOCKS5 TCP CONNECT 监听器。启动时从 DataWorker 读取代理监听地址、端口、最大连接数和启用账号（密码只在 native-only 配置快照中解密，不注册为浏览器 RPC）；监听器在独立线程接受连接，支持 IPv4/IPv6 监听、IPv4/域名/IPv6 CONNECT、可选用户名/密码认证、5 秒目标连接超时、双向 relay 和原子请求/响应/字节/错误计数。停止会关闭监听 socket、等待 accept/client 线程收尾，`getStats` 将真实活动连接和字节计数回传原 Vue。

本切片明确不冒充尚未实现的 HTTP、UDP 或 WPC 协议：HTTP/UDP 配置仍只保留原配置面；`Only_WPC_Client` 开启时启动会给出明确错误，而不是让普通 SOCKS5 客户端绕过限制。代理运行时不执行 SQLite、文件或 UI 操作；账号快照和所有 socket 资源由 native runtime 独占。

新增 `socks5-runtime` 回归：真实本地 TCP 目标分别通过无认证和用户名/密码 SOCKS5 握手，验证 CONNECT、双向 `ping/pong` relay、认证选择、重启/停止和活动/请求/响应/字节计数。x64 与 Win32 均通过；本轮 x64 全量 CTest **21/21**，Win32 SOCKS5 专项通过。机器人运行执行器继续不实现。
